# Day-25 Decay Entry - 2026-09-21T16:01-16:13Z (t + ~480h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Eighteenth post-close confirmation row. Batch `b_1790006490_186c6d9849c9`
(HTTP 202 at 16:01:30Z, queued 8/8 failed 0, rows 16:02:32-16:03:43Z, ids 18882-18889,
app_version 26.51.0-144-g385ba8783 — same binary as Days 6-24). **Every batch verdict
tuple byte-identical to the Days 6-24 canonical state** (dnssec_state / chain_of_trust /
ad_consensus / display_label, all 8 domains). The pq-dualds within-split variant from
Day-24 REVERTED: Quad9 back to `secure` (5/5 completed), restoring the canonical
membership (Quad9/DNS4EU/OpenDNS `secure` + Google/Cloudflare `ad_absent`); consensus
tuple unchanged (3/2 `split`). Per the Day-24 pre-registered rule this folds into the
existing flaky-slot history — one-day variant lifetime, no canonical-membership anchor
note needed. Cloudflare's alg-18 validation held day 21: first AD 2026-09-01T16:03:38Z
-> still AD=True at vantage receipts 16:11:17Z (home), 16:11:20Z (instrument box),
16:13:13Z (Paris) — **~480h, day 21** (480.0h exactly at the 16:03:38Z anchor; 20 days
x 24h; receipts land 8-10 min past the anchor's time-of-day). Google, Quad9, OpenDNS,
DNS4EU silent at the wire (Paris AD=False rows on both windows; controls AD=True on all
three resolvers). Authoritative serials unchanged since Day-0/Day-2. The `:5300`
validator remains DOWN (lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 -> "no
servers could be reached" — Day-12 caveat 1 unchanged, day 15); the authoritative-side
serial check continues as the substitute — receipt in hand, not a claim.

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via fixture box 44.232.227.144 :53 | serial 2026083011 (read 16:09:54Z; unchanged since Day-0) | serial 2026083103 (unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR** (`flags: qr rd ra ad`, 16:13:13Z) — **flip persisted ~480h (day 21)** | **AD=True** (`flags: qr rd ra ad`, 16:13:13Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:13:13Z) | AD=False, NOERROR, silent (16:13:13Z) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:13:13Z) | AD=False, NOERROR, silent (16:13:13Z) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, 16:11:20Z) | **AD=True** (16:11:20Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.218.188.26, 16:11:17Z) | **AD=True** (same) |

Controls: nlnetlabs.nl @1.1.1.1 AD=True from home (16:11:17Z), the instrument box
(16:11:20Z), and Paris (16:13:13Z); Paris controls on 8.8.8.8 and 9.9.9.9 also
AD=True. Serials 2026083011 / 2026083103 byte-verified unchanged since Day-0 /
Day-2 off the fixture box via authoritative :53 (`flags: qr aa` with +norecurse —
authoritative, non-validating, as expected). **Both RRSIGs re-pulled today** from
home (@1.1.1.1 +dnssec): pq SOA RRSIG alg **18** (ML-DSA-44), key tag 33846,
inception 20260830081123, expiry 20260929091123; pq2 RRSIG alg 18, key tag 46383,
inception 20260830232650, expiry 20260930002650 — both within validity, matching
the Days 6-24 receipts. First Cloudflare AD 2026-09-01T16:03:38Z -> still AD=True
2026-09-21 16:11-16:13Z (~480h, day 21).

### Batch verdicts — specimen battery, 8/8 rows landed (batch
`b_1790006490_186c6d9849c9`, HTTP 202 at 16:01:30Z, queued 8/8 failed 0, rows
16:02:32-16:03:43Z, ids 18882-18889; ad_consensus per domain from
`full_results->'dnssec_analysis'`; app_version 26.51.0-144-g385ba8783 — same
binary as Days 6-24)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5 resolvers) | Unconfirmed |
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed |
| pqns.resolutionscope.com | present | inherited | - | Inherited |
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-24 -> Day-25): **pq = all five `ad_absent` (identical);
pq2 = all five `ad_absent` (identical — flaky slot clean, day 3 consecutive);
pq-dualds = Quad9/DNS4EU/OpenDNS `secure` + Google/Cloudflare `ad_absent` — the
Day-24 variant REVERTED (Quad9 `ad_absent` -> `secure`, back to the Days 6-23
canonical membership), consensus tuple unchanged (3/2 `split`);
nlnetlabs.nl = all five `secure` (identical).** The series' single VERDICT-level
resolver transition remains the Day-5 Cloudflare event.

### Day-25 verdict

**The Day-5 event held a twenty-first full day (~480h, day 21).** Cloudflare's
alg-18 validation persisted day 21 (first AD 2026-09-01T16:03:38Z -> still AD=True
2026-09-21 16:11-16:13Z, ~480h — 480.0h exactly at the 16:03:38Z anchor, 20 days x
24h), confirmed from home, the instrument box, and Paris. Google, Quad9, OpenDNS,
DNS4EU did not move at the verdict level. Batch verdict tuples byte-identical to
the Days 6-24 canonical state — stability IS the finding; the Day-24 within-split
Quad9 variant reverted after exactly one day and folds into the flaky-slot history
per the Day-24 pre-registered rule. Authoritative serials unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator still DOWN (Day-12 caveat 1, unchanged, day 15).**
lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 -> "no servers could be
reached" (probed on-box ~16:10Z). Validator repair remains the follow-up task —
the series' only remaining LOCAL validating vantage. Substituted with the
authoritative-side serial check as in Days 12-24.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 20.** Batch rows
(16:02:32-16:03:43Z) again voted Cloudflare `ad_absent` on both alg-18 windows
while direct dig @1.1.1.1 from home (16:11:17Z), the instrument box (16:11:20Z),
and Paris (16:13:13Z) all read AD=True. Analyzer-side, not the wire — now a
20-day-replicated instrument defect, stable across the same binary (-144) on
Days 6-25.
3. **Quad9 flaky-slot watch: NO `unmeasured` recurrence — clean, day 3
consecutive.** Day-12 pq2 `unmeasured`; Day-13 clean; Day-14 pq-dualds; Day-15
clean; Day-16 pq-dualds; Day-17 clean; Day-18 pq-dualds; Day-19 clean; Day-20
clean; Day-21 clean; Day-22 pq2 `unmeasured`; Day-23 clean; Day-24 clean;
Day-25 clean. `unmeasured` recurrences on record remain five (pq2 Day-12/22,
pq-dualds Day-14/16/18). **Day-24 within-split variant DISPOSITIONED today:**
Quad9 `secure` -> `ad_absent` (Day-24) -> `secure` (Day-25) — one-day lifetime,
consensus tuple `split` unchanged throughout; per the Day-24 pre-registered rule
it folds into the existing flaky-slot history (no canonical-membership anchor
note required).
4. **Row-verification keyed two ways, both agree (8/8).** Primary:
`scan_source = 'batch:b_1790006490_186c6d9849c9'` (8 rows). Cross-check:
`full_results->>'_request_source' = 'batch'` (text compare, 8 rows). The
`_request_source` TABLE COLUMN does not exist (42703, carried from Day-11) — and
a NEW quirk recorded today: the jsonb comparison form
`(full_results::jsonb->'_request_source') = 'batch'` throws `invalid input
syntax for type json` because Postgres coerces the untyped `'batch'` literal to
jsonb (DETAIL: Token "batch" is invalid); use the `->>` text form or
`scan_source`. (Second query quirk, same class: `||` and `->` share one
precedence level left-associative — `' :: ' || full_results->'k'` parses as
`(' :: ' || full_results)->'k'` and fails on text; parenthesize the json path.)
`ad_consensus`/`dnssec_state`/`chain_of_trust`/`display_label` live inside
`full_results->'dnssec_analysis'` (json). Inherited rows (dane*, pqns) carry NO
ad_consensus/resolver_ad keys at all — "-" in the table is key-absence, not an
empty vote set.
5. **Mid-flight row-count trap: avoided per the Day-21 standing guidance.** The
first row count fired ~5 min after the HTTP 202 (~16:06-16:07Z) and returned 8/8
immediately — the ~71s staggered-write window (16:02:32 -> 16:03:43Z) had fully
closed.
6. **Egress-labeled receipts (standing requirement) — home egress ROTATED this
run.** Paris 76.13.61.227 (IPv4; same path as Days 7-24), instrument box
44.226.60.249, home 97.218.188.26 (rotated from 97.252.66.138; every vantage
receipt read its egress line before labeling).
7. **SG port-22 allowlists refreshed on BOTH boxes after the egress rotation
(ops note, not a data defect).** dnstool-app sg-06fe9448f84977713: revoke
97.252.66.138/32, authorize 97.218.188.26/32 (same recipe as Day-23 caveat 7).
**Fixture box i-05d5a4ce5b105722f sg-076ef31bb7a84a974: revoke 97.170.246.157/32
(the stale Aug-2 value), authorize 97.218.188.26/32 — fixture-box SSH RESTORED
this run via the dnstool-app pem (ubuntu@), first successful login of the
recorded series, resolving Day-24 caveat 8's follow-up item.** The on-box :5300
lsof probe ran over the restored SSH path. Posture unchanged on both boxes:
port 22 from the current public IP only.
8. **DB access via sudo on the box (ops note, not a defect).** /etc/dnstool/env
is root-only (600); row verification ran through `sudo -n bash -s` with the
DATABASE_URL parsed by grep+cut (direct `source` fails: env line contains
parentheses — bash syntax error; carried from Day-24 caveat 9).
9. **Forward watch (new): pq RRSIG expiry approaches.** pq SOA RRSIG expires
2026-09-29T09:11:23Z (8 days out; pq2 expires 2026-09-30T00:26:50Z, 9 days).
Both within validity today; if the series is still running at expiry, the
windows need a re-sign or the rows will show the expiry honestly. Recorded now
so the transition is not misread as a decay event.
10. **Receipt timestamps: exact where stated, run-order stamps where batched.**
DB row times and the batch fire time (16:01:30Z, batch-id epoch) are exact;
home/box/Paris dig times are `date -u`-stamped per stage (16:11:17-16:13:13Z);
serial read 16:09:54Z. Duration arithmetic (~480h) is insensitive to the
±minutes.

Receipts: Paris dig transcripts (16:13:13Z, AD flags on pq + pq2 + controls on
all three resolvers), fixture-box serial reads via authoritative :53 (`flags: qr
aa`), instrument-box and home dig transcripts (egress-stamped,
16:11:17-16:11:20Z), Cloudflare +dnssec RRSIG receipts for BOTH fixtures (alg
18; key tags 33846 / 46383; expiries 20260929091123 / 20260930002650). DB row
dump + per-resolver votes in the session record, batch
`b_1790006490_186c6d9849c9`. 2026-09-21T16:01-16:13Z.

-- hermes lane, 2026-09-21T16:15Z
