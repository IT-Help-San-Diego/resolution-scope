# Day-23 Decay Entry - 2026-09-19T16:02-16:10Z (t + ~432h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Sixteenth post-close confirmation row. Batch `b_1789833757_3b7e109bd9eb`
(HTTP 202 at 16:02:37Z, queued 8/8 failed 0, rows 16:03:38-16:04:47Z, ids 18865-18872,
app_version 26.51.0-144-g385ba8783 — same binary as Days 6-22). **Every batch verdict
tuple byte-identical to the Days 6-21 canonical state** (dnssec_state / chain_of_trust /
ad_consensus / display_label, all 8 domains). The Quad9 flaky slot was CLEAN today —
pq2 restored to 5/5 `ad_absent` (the Day-22 `unmeasured` did not repeat) and
pq-dualds back at the canonical 3 secure / 2 ad_absent = `split`; recurrences on
record remain five, and both windows read canonical simultaneously. Cloudflare's
alg-18 validation held day 19: first AD 2026-09-01T16:03:38Z -> still AD=True at
vantage receipts 16:03:16Z (home), 16:05:14Z (Paris), 16:09:39Z (instrument box) —
**~432h, day 19** (432.0h exactly at the 16:03:38Z anchor; 18 days x 24h; the home
receipt lands 22s before the anchor's time-of-day, Paris and box receipts after it).
Google, Quad9, OpenDNS, DNS4EU silent at the wire (Paris AD=False rows on both
windows). Authoritative serials unchanged since Day-0/Day-2. The `:5300` validator
remains DOWN (lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 times out —
Day-12 caveat 1 unchanged, day 13); the authoritative-side serial check continues as
the substitute — receipt in hand, not a claim.

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via fixture box 44.232.227.144 :53 | serial 2026083011 (16:03:38Z; unchanged since Day-0) | serial 2026083103 (16:03:38Z; unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR** (`flags: qr rd ra ad`, 16:05:14Z) — **flip persisted ~432h (day 19)** | **AD=True** (`flags: qr rd ra ad`, 16:05:14Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:05:14Z) | AD=False, NOERROR, silent (16:05:14Z) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:05:14Z) | AD=False, NOERROR, silent (16:05:14Z) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, 16:09:39Z) | **AD=True** (16:09:39Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.252.66.138 — ROTATED, see caveat 6, 16:03:16Z) | **AD=True** (same) |

Controls: nlnetlabs.nl @1.1.1.1 AD=True from home (16:03:16Z), Paris (16:05:14Z), and the
instrument box (16:09:39Z). Serials 2026083011 / 2026083103 byte-verified unchanged
since Day-0 / Day-2 off the fixture box via authoritative :53 (`flags: qr aa` with
+norecurse — authoritative, non-validating, as expected). **Both RRSIGs re-pulled
today** from home (@1.1.1.1 +dnssec): pq SOA RRSIG alg **18** (ML-DSA-44), key tag
33846, inception 20260830081123, expiry 20260929091123; pq2 RRSIG alg 18, key tag
46383, inception 20260830232650, expiry 20260930002650 — both within validity,
matching the Days 6-22 receipts. First Cloudflare AD 2026-09-01T16:03:38Z -> still
AD=True 2026-09-19 16:03-16:10Z (~432h, day 19).

### Batch verdicts — specimen battery, 8/8 rows landed (batch
`b_1789833757_3b7e109bd9eb`, HTTP 202 at 16:02:37Z, queued 8/8 failed 0, rows
16:03:38-16:04:47Z, ids 18865-18872; ad_consensus per domain from
`full_results::jsonb->'dnssec_analysis'`; app_version 26.51.0-144-g385ba8783 —
same binary as Days 6-22)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (5/5 resolvers) | Unconfirmed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pqns.resolutionscope.com | present | inherited | - | Inherited |
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-22 -> Day-23): **pq = all five `ad_absent` (identical);
pq2 restored to 5/5 `ad_absent` — the flaky slot CLEANED today** (Day-22 was
Quad9 `unmeasured`, 4/5); pq-dualds = Quad9/DNS4EU/OpenDNS `secure` + Google/
Cloudflare `ad_absent` (canonical split, unchanged); nlnetlabs.nl = all five
`secure`. The series' single VERDICT-level resolver transition remains the Day-5
Cloudflare event.

### Day-23 verdict

**The Day-5 event held a nineteenth full day (~432h, day 19).** Cloudflare's
alg-18 validation persisted day 19 (first AD 2026-09-01T16:03:38Z -> still
AD=True 2026-09-19 16:03-16:10Z, ~432h — 432.0h exactly at the 16:03:38Z
anchor, 18 days x 24h; receipts bracket the anchor), confirmed from home, the
instrument box, and Paris. Google, Quad9, OpenDNS, DNS4EU did not move at the
verdict level. Batch verdict tuples byte-identical to the Days 6-21 canonical
state — stability IS the finding, and today's clean flaky slot (both windows
canonical simultaneously) is the ordinary state restored, recorded as such.
Authoritative serials unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator still DOWN (Day-12 caveat 1, unchanged, day 13).**
lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 times out. Validator
repair remains the follow-up task — it is the series' only remaining LOCAL
validating vantage. Substituted with the authoritative-side serial check as in
Days 12-22.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 18.** Batch
rows (16:03:38-16:04:47Z) again voted Cloudflare `ad_absent` on both alg-18
windows while direct dig @1.1.1.1 from home (16:03:16Z), Paris (16:05:14Z), and the
instrument box (16:09:39Z) all read AD=True. Analyzer-side, not the wire — now an
18-day-replicated instrument defect, stable across the same binary (-144) on
Days 6-23.
3. **Quad9 flaky-slot watch: CLEAN today — both windows canonical.** Day-12
pq2 `unmeasured`; Day-13 clean; Day-14 pq-dualds; Day-15 clean; Day-16
pq-dualds; Day-17 clean; Day-18 pq-dualds; Day-19 clean; Day-20 clean;
Day-21 clean; Day-22 pq2 `unmeasured`; Day-23 clean. Recurrences on record
remain five (pq2 Day-12/22, pq-dualds Day-14/16/18); the consensus verdict
(`ad_absent` on pq2, `split` on pq-dualds) held through every occurrence.
The strict day-on/day-off alternation broken by the Days 19-21 clean run
stays broken; today is the first clean day after the Day-22 recurrence.
4. **Row-verification keyed on `scan_source`** (value `batch:<batch_id>`),
not `_request_source` (carried from Day-11; the prompt-specified
`_request_source` column re-threw 42703 when re-probed today — one cheap
attempt, the Day-11/17/19 reproductions now also stand at Day-23);
`ad_consensus`/`dnssec_state`/`chain_of_trust`/`display_label` live inside
`full_results->'dnssec_analysis'` (json — `::jsonb` cast used); `display_label`
is a KEY inside that JSON, not a table column (information_schema check
recorded at Day-21). 8/8 rows found for this batch_id (ids 18865-18872); the
15-minute-window cross-check (`scan_source LIKE 'batch:%'`) returned 8.
5. **Mid-flight row-count trap: avoided per the Day-21 standing guidance.**
The row count fired ~5.5 min after the HTTP 202 (~16:08Z) and returned 8/8
immediately — the ~69s staggered-write window (16:03:38 -> 16:04:47Z) had
fully closed. Standing guidance (wait ~3 min after the 202 before the first
row count) held.
6. **Egress-labeled receipts (standing requirement) — HOME EGRESS ROTATED.**
Paris 76.13.61.227 (IPv4; same path as Days 7-22), instrument box
44.226.60.249, home **97.252.66.138** (was 97.170.246.157 through Day-22;
ISP rotation overnight) — every vantage receipt read its egress line before
labeling (`curl -4` used throughout; dual-stack vantages, IPv4 labels kept).
7. **dnstool-app SG port-22 allowlist was stale after the egress rotation —
found and fixed this run (ops note, not a data defect).** SSH to dnstool-app
timed out twice (~16:04-16:06Z); root cause confirmed by SG read:
`dnstool-app-sg` (sg-06fe9448f84977713) still allowed only `97.170.246.157/32`
on tcp/22. Fix applied add-first to keep a live path at all times:
authorize `97.252.66.138/32` (16:06Z, sgr-01fd5a963dd041c8a) -> SSH verified
(16:07:13Z) -> revoke stale `97.170.246.157/32` (16:07Z, sgr-076ea09c18b198808)
-> post-revoke SSH verified. End state = the documented posture (port 22 from
the current public IP only). **The battery itself was unaffected** — the HTTP
202 landed at 16:02:37Z over 443 (world-open) before any SSH attempt, and row
writes completed normally. Standing consequence for future runs: if the home
egress rotates again, SSH breaks the same way; the SG refresh (add new /32,
verify, revoke old) is the repair recipe, now documented.
8. **Fixture-box SSH still denied (Day-14 caveat 6, unchanged).** Not retried
today; the serial reads again went over DNS to the authoritative :53
(44.232.227.144, `flags: qr aa`). Measurement unaffected. SSH access to
the fixture box remains a follow-up item, not a data defect.
9. **DB access via sudo on the box (ops note, not a defect).** /etc/dnstool/env
is root-only (600); row verification ran through `sudo -n bash -s` with the
DATABASE_URL parsed by grep+cut (direct `source` fails: env line 8 contains
parentheses — bash syntax error). Documented so future rows don't re-derive
the access path.
10. **Receipt timestamps: exact where stated, run-order estimates where `~`.**
DB row times and the batch fire time (16:02:37Z) are exact; the home, box,
Paris, and serial-read dig times are stamped by `date -u` at each stage
(16:03:16-16:09:39Z) — order-accurate, not per-dig timestamped. Duration
arithmetic (~432h) is insensitive to the estimate (±minutes against a
432-hour window; the 432.0h figure is computed from the exact Day-0 anchor at
16:03:38Z).

Receipts: Paris dig transcripts (16:05:14Z, AD flags on pq + pq2 + control +
Google/Quad9 silent rows), fixture-box serial reads via authoritative :53
(`flags: qr aa`), instrument-box and home dig transcripts (egress-stamped,
16:03:16-16:09:39Z), Cloudflare +dnssec RRSIG receipts for BOTH fixtures (alg 18;
key tags 33846 / 46383; expiries 20260929091123 / 20260930002650). DB row
dump + per-resolver votes in the session record, batch
`b_1789833757_3b7e109bd9eb`. 2026-09-19T16:02-16:10Z.

-- hermes lane, 2026-09-19T16:10Z
