# Day-28 Decay Entry - 2026-09-24T16:04-16:07Z (t + ~552h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Twenty-first post-close confirmation row. Batch `b_1790265843_c5fea97d31aa`
(HTTP 202 at 16:04:03Z — batch-id epoch; curl window opened 16:04:03Z, queued 8/8 failed 0, rows
16:05:03-16:06:13Z, ids 18915-18922, app_version 26.51.0-144-g385ba8783 — same binary as Days
6-27). **Every batch verdict tuple byte-identical to the Days 6-27 canonical state**
(dnssec_state / chain_of_trust / ad_consensus / display_label, all 8 domains) — but a **within-
split flaky-slot variant fired on pq-dualds: Quad9 `unmeasured`** (6th recurrence on record), so
the canonical membership did NOT hold today (the Day-25-27 Quad9-`secure` streak broke at day 3);
consensus tuple unchanged (`split`). Cloudflare's alg-18 validation held day 24: first AD
2026-09-01T16:03:38Z -> still AD=True at vantage receipts 16:04:22Z (home), 16:04:44Z (instrument
box), 16:06:39Z (Paris) — **~552h, day 24** (552.0h exactly at the 16:03:38Z anchor; 23 days x
24h; unlike Days 6-27 the receipts fall AFTER the anchor's time-of-day today — home +44s, the box
+1m06s, Paris +3m01s — rather than bracketing it). Google, Quad9, OpenDNS, DNS4EU silent at the
wire (Paris AD=False rows on both windows; controls AD=True on all three resolvers).
Authoritative serials unchanged since Day-0/Day-2. The `:5300` validator remains DOWN (lsof
empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 -> "no servers could be reached" — Day-12
caveat 1 unchanged, day 18, probed over the Day-25-restored fixture-box SSH path); the
authoritative-side serial check continues as the substitute — receipt in hand, not a claim.

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via fixture box 44.232.227.144 :53 | serial 2026083011 (read 16:05:17Z; unchanged since Day-0) | serial 2026083103 (unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR** (`flags: qr rd ra ad`, 16:06:39Z) — **flip persisted ~552h (day 24)** | **AD=True** (`flags: qr rd ra ad`, 16:06:41Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:06:42Z) | AD=False, NOERROR, silent (16:06:42Z) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:06:43Z) | AD=False, NOERROR, silent (16:06:44Z) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, 16:04:44Z) | **AD=True** (16:04:44Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.218.188.26, 16:04:22Z) | **AD=True** (16:04:23Z) |

Controls: nlnetlabs.nl @1.1.1.1 AD=True from home (16:04:23Z), the instrument box (16:04:44Z),
and Paris (16:06:41Z); Paris controls on 8.8.8.8 and 9.9.9.9 also AD=True (16:06:43/16:06:45Z).
Serials 2026083011 / 2026083103 byte-verified unchanged since Day-0 / Day-2 off the fixture box
via authoritative :53 (`flags: qr aa` with +norecurse — authoritative, non-validating, as
expected). **Both RRSIGs re-pulled today** from home (@1.1.1.1 +dnssec): pq SOA RRSIG alg **18**
(ML-DSA-44), key tag 33846, inception 20260830081123, expiry 20260929091123; pq2 RRSIG alg 18,
key tag 46383, inception 20260830232650, expiry 20260930002650 — both within validity, matching
the Days 6-27 receipts byte-for-byte. First Cloudflare AD 2026-09-01T16:03:38Z -> still AD=True
2026-09-24 16:04-16:07Z (~552h, day 24).

### Batch verdicts — specimen battery, 8/8 rows landed (batch
`b_1790265843_c5fea97d31aa`, HTTP 202 at 16:04:03Z, queued 8/8 failed 0, rows
16:05:03-16:06:13Z, ids 18915-18922; ad_consensus per domain from
`full_results->'dnssec_analysis'`; app_version 26.51.0-144-g385ba8783 — same
binary as Days 6-27)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (5/5 resolvers) | Unconfirmed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (2 secure / 2 ad_absent / 1 unmeasured) | Unconfirmed |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| pqns.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-27 -> Day-28): **pq = all five `ad_absent` (identical);
pq2 = all five `ad_absent` (identical — flaky slot clean); pq-dualds =
Cloudflare/Google `ad_absent` + DNS4EU/OpenDNS `secure` + **Quad9 `unmeasured`**
— the flaky slot fired on pq-dualds, so the canonical membership did NOT hold
(day-3 streak broken); consensus tuple unchanged (`split`); nlnetlabs.nl = all
five `secure` (identical).** The series' single VERDICT-level resolver transition
remains the Day-5 Cloudflare event.

### Day-28 verdict

**The Day-5 event held a twenty-fourth full day (~552h, day 24).** Cloudflare's
alg-18 validation persisted day 24 (first AD 2026-09-01T16:03:38Z -> still AD=True
2026-09-24 16:04-16:07Z, ~552h — 552.0h exactly at the anchor, 23 days x 24h;
receipts fall after the anchor's time-of-day today), confirmed from home, the
instrument box, and Paris. Google, Quad9, OpenDNS, DNS4EU did not move at the
verdict level. Batch verdict tuples byte-identical to the Days 6-27 canonical
state — stability IS the finding — with one within-split flaky-slot variant:
Quad9 `unmeasured` on pq-dualds (6th recurrence on record; consensus tuple
`split` unchanged; watch tomorrow for the canonical membership to restore per the
established one-day pattern). Authoritative serials unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator still DOWN (Day-12 caveat 1, unchanged, day 18).**
lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 -> "no servers could be
reached" (probed on-box 16:04:44Z). Validator repair remains the follow-up task —
the series' only remaining LOCAL validating vantage. Substituted with the
authoritative-side serial check as in Days 12-27. The probe ran over the
Day-25-restored fixture-box SSH path, which worked first try again.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 23.** Batch rows
(16:05:03-16:06:13Z) again voted Cloudflare `ad_absent` on both alg-18 windows
while direct dig @1.1.1.1 from home (16:04:22Z), the instrument box (16:04:44Z),
and Paris (16:06:39Z) all read AD=True. Analyzer-side, not the wire — now a
23-day-replicated instrument defect, stable across the same binary (-144) on
Days 6-28.
3. **Quad9 flaky-slot watch: RECURRED — pq-dualds `unmeasured` (6th recurrence).**
Day-12 pq2 `unmeasured`; Day-13 clean; Day-14 pq-dualds; Day-15 clean; Day-16
pq-dualds; Day-17 clean; Day-18 pq-dualds; Day-19 clean; Day-20 clean; Day-21
clean; Day-22 pq2 `unmeasured`; Day-23 clean; Day-24 clean; Day-25 clean; Day-26
clean; Day-27 clean; Day-28 pq-dualds `unmeasured`. Recurrences on record now
six (pq2 Day-12/22, pq-dualds Day-14/16/18/28). Consequence today: the
pq-dualds canonical membership (Quad9 `secure`, held Days 25-27, day-3 streak)
did NOT hold — votes are 2 `secure` / 2 `ad_absent` / Quad9 `unmeasured`; the
consensus tuple `split` is unchanged. The Day-24 within-split verdict-level
variant disposition stands as final (folded into flaky-slot history). Watch
tomorrow for the canonical membership to restore per the established one-day
pattern; if Quad9's vote at the canonical slot stays `unmeasured` a second day,
that escalates the slot from flaky to suspect.
4. **Row-verification keyed two ways, both agree (8/8).** Primary:
`scan_source = 'batch:b_1790265843_c5fea97d31aa'` (8 rows). Cross-check:
`full_results->>'_request_source' = 'batch'` (text compare, 8 rows; the
15-minute-window form from the Day-0 tasking works only as this text compare —
the `_request_source` TABLE COLUMN does not exist, 42703, carried from Day-11;
the jsonb-coercion form `(full_results::jsonb->'_request_source') = 'batch'`
throws `invalid input syntax for type json` (Day-25); `||` and `->` share one
precedence level — parenthesize json paths (Day-25). `ad_consensus`/
`dnssec_state`/`chain_of_trust`/`display_label` live inside
`full_results->'dnssec_analysis'` (json). Inherited rows (dane*, pqns) carry NO
ad_consensus/resolver_ad keys at all — "-" in the table is key-absence, not an
empty vote set.
5. **Mid-flight row-count trap: FIRED THIS RUN — the Day-21 standing guidance
caught it, zero harm.** The first count fired between 16:05:12Z (after row 18918
wrote) and 16:06:05Z (before row 18919 wrote) — ~1-2 min after the HTTP 202,
inside the staggered-write window — and returned 4/8 (ids 18915-18918 only).
The recount at 16:06:42Z (window fully closed: rows span 16:05:03.2 ->
16:06:13.3Z, ~70s) returned 8/8 immediately. Standing guidance (count >= 3 min
after the 202) vindicated again; the transient 4/8 was a mid-flight read, not a
failed queue (the batch endpoint had already reported queued 8/8 failed 0).
6. **Egress-labeled receipts (standing requirement) — no rotation this run.**
Paris 76.13.61.227 (IPv4; same path as Days 7-27), instrument box 44.226.60.249,
home 97.218.188.26 (same as Days 25-27; every vantage receipt read its egress line
before labeling, `curl -4` throughout; dual-stack vantages, IPv4 labels kept).
7. **SG port-22 allowlists: no repair needed this run (ops note, not a data
defect).** Home egress stable at 97.218.188.26 (Days 25-27); dnstool-app SSH
verified first try (16:04:44Z); fixture-box SSH verified first try from home via
the dnstool-app-2026.pem (16:05:17Z). Path clarification carried from Day-27: the
dnstool-app box itself carries only `authorized_keys` in ~/.ssh — no pem there;
the fixture-box path is home-side with ~/.ssh/dnstool-app-2026.pem. Posture
unchanged on both boxes: port 22 from the current public IP only. Standing
consequence unchanged: if the home egress rotates again, SSH breaks the same
way; the SG refresh recipe is documented at Day-23 caveat 7.
8. **DB access via sudo on the box (ops note, not a defect).** /etc/dnstool/env
is root-only (600); row verification ran through `sudo -n bash -s` with the
DATABASE_URL parsed by grep+cut (direct `source` fails: env line contains
parentheses — bash syntax error; carried from Day-24 caveat 9).
9. **Forward watch (carried from Day-25): pq RRSIG expiry approaches.** pq SOA
RRSIG expires 2026-09-29T09:11:23Z (5 days out; pq2 expires 2026-09-30T00:26:50Z,
6 days). Both within validity today (receipts re-pulled, byte-matching the
Days 6-27 values); if the series is still running at expiry, the windows need a
re-sign or the rows will show the expiry honestly. Carried so the transition is
not misread as a decay event.
10. **Receipt timestamps: exact where stated, run-order stamps where batched.**
DB row times, the batch fire time (16:04:03Z, batch-id epoch), the :5300 probe
(16:04:44Z), the fixture-box serial reads (16:05:17Z), and the DB-verify recount
window (T0 16:06:42Z) are exact; home/box/Paris dig times are `date -u`-stamped
per stage (16:04:22-16:06:45Z); the first (mid-flight) count is bounded, not
stamped — see caveat 5. Duration arithmetic (~552h) is insensitive to the
±minutes.

Receipts: Paris dig transcripts (16:06:39-16:06:45Z, AD flags on pq + pq2 +
controls on all three resolvers), fixture-box serial reads via authoritative :53
(`flags: qr aa`, 16:05:17Z), instrument-box and home dig transcripts
(egress-stamped, 16:04:22-16:04:44Z), Cloudflare +dnssec RRSIG receipts for BOTH
fixtures (alg 18; key tags 33846 / 46383; expiries 20260929091123 /
20260930002650), on-box :5300 lsof+dig probe transcript (16:04:44Z). DB row dump
+ per-resolver votes in the session record, batch `b_1790265843_c5fea97d31aa`.
2026-09-24T16:04-16:07Z.

-- hermes lane, 2026-09-24T16:10Z
