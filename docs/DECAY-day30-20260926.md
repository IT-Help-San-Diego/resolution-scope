# Day-30 Decay Entry - 2026-09-26T16:02-16:06Z (t + ~600h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Twenty-third post-close confirmation row. Batch `b_1790438522_6eba3c3398de`
(HTTP 202 at 16:02:02Z — batch-id epoch; queued 8/8 failed 0, rows 16:03:02-16:04:13Z, ids
18936-18943, app_version 26.51.0-144-g385ba8783 — same binary as Days 6-29). **Every batch verdict
tuple byte-identical to the Days 6-29 canonical state** (dnssec_state / chain_of_trust /
ad_consensus / display_label, all 8 domains) — and the **within-split flaky slot MOVED BACK**: a
Quad9 `unmeasured` variant fired on **pq-dualds** (5th pq-dualds recurrence, 8th on record) while
**pq2 restored to full canonical membership** (all five resolvers `ad_absent`) — the Day-29
escalation watch resolved exactly per the one-day pattern, NO escalation; both consensus tuples
unchanged (`ad_absent` / `split`). Cloudflare's alg-18 validation held day 26: first AD
2026-09-01T16:03:38Z -> still AD=True at vantage receipts 16:03:53Z (instrument box),
16:04:55Z (home), 16:05:48Z (Paris) — **~600h, day 26** (600.0h at the anchor's time-of-day;
25 days x 24h). Google, Quad9, OpenDNS, DNS4EU silent at the wire (Paris AD=False rows on the two
non-Cloudflare resolvers; controls AD=True on all three resolvers). Authoritative serials
unchanged since Day-0/Day-2. The `:5300` validator remains DOWN (lsof empty on TCP and UDP 5300;
dig @127.0.0.1 -p 5300 -> "no servers could be reached" — Day-12 caveat 1 unchanged, day 20); the
authoritative-side serial check continues as the substitute — receipt in hand, not a claim. One
**labeled instrument-procedure incident** (caveat 6): the first vantage pass was fired with
`+norecurse` against the recursive resolvers and produced a storm of SERVFAIL (Cloudflare/Google)
and REFUSED (Quad9) on every target INCLUDING the control — caught by the control, corrected
inside the same receipt window; all canonical receipts above are from the corrected pass.

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via fixture box 44.232.227.144 :53 | serial 2026083011 (read 16:04:07Z; unchanged since Day-0) | serial 2026083103 (unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR** (`flags: qr rd ra ad`, 16:05:48Z) — **flip persisted ~600h (day 26)** | **AD=True** (`flags: qr rd ra ad`, 16:05:48Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:05:48Z) | AD=False, NOERROR, silent (16:05:48Z) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:05:48Z) | AD=False, NOERROR, silent (16:05:48Z) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, 16:03:53Z) | **AD=True** (16:03:53Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.218.188.26, 16:04:55Z) | **AD=True** (16:04:55Z) |

Controls: nlnetlabs.nl @1.1.1.1 AD=True from home (16:04:55Z), the instrument box (16:03:53Z),
and Paris (16:05:48Z); Paris controls on 8.8.8.8 and 9.9.9.9 also AD=True (16:05:48Z).
Serials 2026083011 / 2026083103 byte-verified unchanged since Day-0 / Day-2 off the fixture box
via authoritative :53 (`flags: qr aa` with +norecurse — authoritative, non-validating, as
expected). **Both RRSIGs re-pulled today** from home (@1.1.1.1 +dnssec, 16:04:55Z): pq SOA RRSIG
alg **18** (ML-DSA-44), key tag 33846, inception 20260830081123, expiry 20260929091123; pq2
RRSIG alg 18, key tag 46383, inception 20260830232650, expiry 20260930002650 — both within
validity, key fields matching the Days 6-29 receipts byte-for-byte. First Cloudflare AD
2026-09-01T16:03:38Z -> still AD=True 2026-09-26 16:02-16:06Z (~600h, day 26).

### Batch verdicts — specimen battery, 8/8 rows landed (batch
`b_1790438522_6eba3c3398de`, HTTP 202 at 16:02:02Z, queued 8/8 failed 0, rows
16:03:02-16:04:13Z, ids 18936-18943; ad_consensus per domain from
`full_results->'dnssec_analysis'`; app_version 26.51.0-144-g385ba8783 — same
binary as Days 6-29)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (5/5 resolvers) | Unconfirmed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (2 secure / 2 ad_absent / 1 unmeasured) | Unconfirmed |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| pqns.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-29 -> Day-30): **pq = all five `ad_absent` (identical);
pq2 = all five `ad_absent` — Quad9 restored to canonical membership after its
Day-29 `unmeasured` firing, the one-day pattern held, NO escalation (the
Day-29 watch resolved as written); pq-dualds = Cloudflare/Google `ad_absent` +
DNS4EU/OpenDNS `secure` + **Quad9 `unmeasured`** — the flaky slot MOVED BACK to
pq-dualds, consensus tuple `split` unchanged; nlnetlabs.nl = all five `secure`
(identical).** The series' single VERDICT-level resolver transition remains the
Day-5 Cloudflare event.

### Day-30 verdict

**The Day-5 event held a twenty-sixth full day (~600h, day 26).** Cloudflare's
alg-18 validation persisted day 26 (first AD 2026-09-01T16:03:38Z -> still
AD=True 2026-09-26 16:02-16:06Z, ~600h — 600.0h at the anchor's time-of-day,
25 days x 24h), confirmed from home, the instrument box, and Paris. Google,
Quad9, OpenDNS, DNS4EU did not move at the verdict level. Batch verdict tuples
byte-identical to the Days 6-29 canonical state — stability IS the finding —
with the within-split flaky slot MOVED BACK from pq2 (restored to canonical
full membership, Day-29 escalation watch resolved with no escalation) to
pq-dualds (Quad9 `unmeasured`, 8th recurrence on record; consensus tuple
`split` unchanged). No wire-level transients today; one labeled
instrument-procedure incident caught by the control (caveat 6). Authoritative
serials unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator still DOWN (Day-12 caveat 1, unchanged, day 20).**
lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 -> "no servers could be
reached" (probed on-box 16:02-16:03Z). Validator repair remains the follow-up
task — the series' only remaining LOCAL validating vantage. Substituted with
the authoritative-side serial check as in Days 12-29. The probe ran over the
Day-25-restored fixture-box SSH path, which worked first try again.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 25.** Batch rows
(16:03:02-16:04:13Z) again voted Cloudflare `ad_absent` on both alg-18 windows
while direct dig @1.1.1.1 from home (16:04:55Z), the instrument box
(16:03:53Z), and Paris (16:05:48Z) all read AD=True. Analyzer-side, not the
wire — now a 25-day-replicated instrument defect, stable across the same
binary (-144) on Days 6-30.
3. **Quad9 flaky-slot watch: MOVED BACK — pq2 restored, pq-dualds fired.**
Day-12 pq2 `unmeasured`; Day-13 clean; Day-14 pq-dualds; Day-15 clean; Day-16
pq-dualds; Day-17 clean; Day-18 pq-dualds; Day-19 clean; Day-20 clean; Day-21
clean; Day-22 pq2 `unmeasured`; Day-23 clean; Day-24 clean; Day-25 clean;
Day-26 clean; Day-27 clean; Day-28 pq-dualds `unmeasured`; Day-29 pq2
`unmeasured`; Day-30 **pq-dualds `unmeasured`**. Recurrences on record now
EIGHT (pq2 Day-12/22/29, pq-dualds Day-14/16/18/28/30). Consequence today:
pq2's Quad9 vote restored `ad_absent` — the Day-29 escalation watch ("if Quad9
stays `unmeasured` there a second day, escalate pq2 from flaky to suspect")
resolved with NO escalation, one-day pattern held; pq-dualds Quad9 went
`unmeasured`, consensus tuple `split` unchanged. Escalation watch now applies
to the pq-dualds slot: if Quad9 stays `unmeasured` there a second day, that
escalates pq-dualds from flaky to suspect.
4. **Row-verification keyed two ways, both agree (8/8).** Primary:
`scan_source = 'batch:b_1790438522_6eba3c3398de'` (8 rows). Cross-check:
`full_results->>'_request_source' = 'batch'` (text compare, 8 rows; the
15-minute-window form from the Day-0 tasking works only as this text compare —
the `_request_source` TABLE COLUMN does not exist, 42703, carried from Day-11;
the jsonb-coercion form `(full_results::jsonb->'_request_source') = 'batch'`
throws `invalid input syntax for type json` (Day-25); `||` and `->` share one
precedence level — parenthesize json paths (Day-25). `ad_consensus`/
`dnssec_state`/`chain_of_trust`/`display_label` live inside
`full_results->'dnssec_analysis'` (json). Inherited rows (dane*, pqns) carry NO
ad_consensus/resolver_ad keys at all — "-" in the table is key-absence, not an
empty vote set.
5. **Mid-flight row-count trap: did NOT fire this run.** The count fired
~16:05Z — ~3m after the 202 (16:02:02Z), AFTER the staggered-write window fully
closed (rows span 16:03:02 -> 16:04:13Z, ~71s) — and returned 8/8 immediately.
The Day-21 standing guidance (count >= 3 min after the 202) continues to hold.
6. **NEW labeled instrument-procedure incident: +norecurse probe storm
(agent-side query construction, NOT the wire).** The first vantage pass ran
`dig ... +norecurse` against the RECURSIVE resolvers: SERVFAIL from Cloudflare
and Google on every target, REFUSED from Quad9 (documented Quad9 behavior on
non-RD queries), on ALL domains INCLUDING the control nlnetlabs.nl, from all
three vantages (16:02-16:05Z). Caught by the control — a zone-level event
cannot fail a healthy control — diagnosed as query construction, and the full
vantage set was re-run with RD set inside the same receipt window; every
canonical reading above is from the corrected pass. Two records for the
future: (a) the control did exactly its job — this is the "could this command
succeed while measuring nothing" discipline catching an apparatus error before
it became a false decay event; (b) a naive comparison against Day-29's single
home SERVFAIL transient (Day-29 caveat 6) could have misread this storm as
"the transient recurred at scale" — the control failure is the discriminator.
Day-29's one-off transient did NOT recur today (home receipts clean with RD
set; transient counter stays at 1, first clean day since). Quad9's
REFUSED-on-nonRD behavior is now on record so a future REFUSED row from a
correct RD-set pass is recognizable as anomalous rather than explained away.
7. **Egress-labeled receipts (standing requirement) — no rotation this run.**
Paris 76.13.61.227 (IPv4; same path as Days 7-29), instrument box 44.226.60.249,
home 97.218.188.26 (same as Days 25-29; every vantage receipt read its egress line
before labeling, `curl -4` throughout; dual-stack vantages, IPv4 labels kept).
8. **SG port-22 allowlists: no repair needed this run (ops note, not a data
defect).** Home egress stable at 97.218.188.26 (Days 25-29); dnstool-app SSH
verified first try (16:03:53Z); fixture-box SSH verified first try from home via
the dnstool-app-2026.pem (16:04:07Z). Path clarification carried from Day-27: the
dnstool-app box itself carries only `authorized_keys` in ~/.ssh — no pem there;
the fixture-box path is home-side with ~/.ssh/dnstool-app-2026.pem. Posture
unchanged on both boxes: port 22 from the current public IP only. Standing
consequence unchanged: if the home egress rotates again, SSH breaks the same
way; the SG refresh recipe is documented at Day-23 caveat 7.
9. **DB access via sudo on the box (ops note, not a defect) + a NEW credentials
hygiene sub-lesson.** /etc/dnstool/env is root-only (600); row verification ran
through `sudo -n bash -s` with the DATABASE_URL parsed by grep+cut (direct
`source` fails: env line contains parentheses — bash syntax error; carried from
Day-24 caveat 9). Sub-lesson: the FIRST query attempt wrapped the psql
invocation in `eval`, and bash's syntax-error echo printed the full
DATABASE_URL (credentials) into the session log — re-ran immediately without
eval (direct quoted variable, never echoed or logged). No repo or public
surface was involved (session transcript only), but `eval` around
secrets-bearing lines is now banned in this lane's recipes; the corrected
no-eval form is the standing recipe.
10. **Forward watch (carried, updated countdown): pq RRSIG expiry approaches.**
pq SOA RRSIG expires 2026-09-29T09:11:23Z (**3 days out**; pq2 expires
2026-09-30T00:26:50Z, **4 days out**). Both within validity today (receipts
re-pulled at 16:04:55Z, key fields byte-matching the Days 6-29 values); if the
series is still running at expiry, the windows need a re-sign or the rows will
show the expiry honestly. Carried so the transition is not misread as a decay
event.

Receipts: Paris dig transcripts (16:05:48Z, AD flags on pq + pq2 + controls on
all three resolvers), fixture-box serial reads via authoritative :53 (`flags: qr
aa`, 16:04:07Z), instrument-box and home dig transcripts (egress-stamped,
16:02-16:06Z), Cloudflare +dnssec RRSIG receipts for BOTH fixtures (alg 18;
key tags 33846 / 46383; expiries 20260929091123 / 20260930002650), on-box :5300
lsof+dig probe transcript (16:02-16:03Z). DB row dump + per-resolver votes in the
session record, batch `b_1790438522_6eba3c3398de`. 2026-09-26T16:02-16:06Z.

-- hermes lane, 2026-09-26T16:10Z
