# seL4 Demo — Toolchain Verified + QEMU GIC Finding

**Date:** 2026-08-22 (overnight session, continued)
**Author:** Hermes lane (instrument/backend)
**Status:** full toolchain VERIFIED end-to-end; one QEMU runtime gap remains

## 1. Toolchain assembled + verified on the Beelink (lab-pc)

Every prerequisite is now installed and PROVEN working (not just present):

| Component | Version | Verified |
|---|---|---|
| ARM bare-metal toolchain | `aarch64-none-elf-gcc` 12.3.Rel1 | `12.3.1 20230626` (compiled hello.c) |
| LionsOS tree | `au-ts/lionsos` + all submodules | `git clone` + `git submodule update --init` |
| Microkit SDK | **2.3.0** (pre-built `linux-x86-64`) | **GPG Good signature** from Julia Vassiliki, fp `FE91 4864 43B0 F4EB 9ECC 3652 4D86 8A34 EDF3 FDCA` (key from keys.openpgp.org) |
| QEMU | 8.2.2 (`qemu-system-aarch64`) | boots the image |
| seL4 kernel | built into the SDK board dirs | boots to "Bootstrapping kernel" |

## 2. End-to-end build path PROVEN

The Microkit "hello" example built and booted:

```
aarch64-none-elf-gcc -c -nostdlib -ffreestanding ... hello.c -o hello.o
aarch64-none-elf-ld ... hello.o -lmicrokit -Tmicrokit.ld -o hello.elf
bin/microkit hello.system ... -o loader.img
  MICROKIT|CAPDL SPEC: number of root objects = 34, spec footprint = 4.92 KiB
  MICROKIT|LOADER: image file size = 2.62 MiB
```

This confirms the FULL contract for our compartment: `.system` XML →
`aarch64-none-elf-gcc` component ELF → `bin/microkit` tool → capDL generated
internally → bootable `loader.img`. The hand-written `.cdl` is confirmed
obsolete (capDL is generated by the microkit tool, not authored).

## 3. QEMU runtime finding — RESOLVED (2026-08-22)

The GIC data abort was a **wrong QEMU invocation**, not a kernel defect. The
Microkit manual (`doc/microkit_user_manual.pdf` §8.13 "QEMU virt (AArch64)")
gives the canonical command, which differs from the obvious `-kernel` form in
two load-bearing ways:

1. The image is loaded via `-device loader,file=<img>,addr=0x70000000,cpu-num=0`
   (NOT `-kernel`). `-kernel` places the image at a different address and the
   kernel's GIC mapping lands wrong → the `KERNEL DATA ABORT` at the GIC.
2. `-m size=2G` — the board is built for **2GB** main memory (the
   `platform_gen.json` "memory" entry of 0x60000000..0xc0000000 is the kernel's
   *available* region, NOT the QEMU memory size; reading it as "1.5GiB" and
   passing `-m 1536` was a red herring).

Canonical command (verbatim from the manual):

```
qemu-system-aarch64 \
    -machine virt,virtualization=on \
    -cpu cortex-a53 \
    -nographic \
    -serial mon:stdio \
    -device loader,file=[SYSTEM IMAGE],addr=0x70000000,cpu-num=0 \
    -m size=2G
```

**Verified result** — the hello example boots and prints:

```
Booting all finished, dropped to user space
INFO  [sel4_capdl_initializer::initialize] Starting CapDL initializer
MON|INFO: Microkit Monitor started!
hello, world
```

The `"Could not infer GIC interrupt target ID, assuming 0"` warning is benign
(it appears in successful boots too; it is seL4's GICv2 target-id inference
logging, not the fault source). The whole seL4 → CapDL → Microkit → PD chain is
now proven running on the Beelink.

## 4. Status — the demo now BOOTS and passes a verdict (2026-08-22)

The two-PD store-compartment system is **authored, built, and proven running**
under QEMU (`native/microkit/`):

```
store: init (passive, no network, no irq)
MON|INFO: PD 'store' is now passive!
engine: init (stub, sends one verdict)
store: received verdict on channel (sealed)
```

The passive store PD holds **no `<irq>`, no network `<map>`, no network
`<memory_region>`** — only the IPC `<channel>` from the engine stub — so "no
network capability" is a structural (kernel-enforced) property, exactly the
§3 theorem's shape. The store is a C shim (the SDK's proven `init()`/`notified()`
template); the no_std Rust compartment (which does the real seal re-derivation)
is the next wiring step, not a shape change.

### Remaining (in order)

1. **Wire the Rust compartment as the store PD** — replace `store.c` with the
   no_std Rust `[[bin]]` (already links bare-metal) via a Microkit Rust binding
   or a C shim that calls into it. The real work (receive ScoredAnalysis,
   re-derive SHA3-512 seal, verify, render) is already host-tested in the
   native crate; only the Microkit→Rust call is missing.
2. **Engine side** — under Option B the real engine is the std Rust binary on
   the Linux host; the demo's `engine.c` stub is the placeholder until the
   host→seL4 verdict handoff is defined.
3. **Optional**: boot-to-print already proves the chain; the seal-over-IPC
   round-trip (send a real sealed verdict across the channel and verify inside
   the store) is the natural next acceptance check.

## 5. Builder facts (for the next session)

- `lab-pc` (192.168.1.157), x86_64, Ubuntu 24.04.
- Toolchain at `/opt/toolchain/arm-gnu-toolchain-12.3.rel1-x86_64-aarch64-none-elf/bin`
  (must be on PATH for builds).
- Microkit SDK at `~/microkit-sdk/microkit-sdk-2.3.0` (`MICROKIT_SDK` env var).
- LionsOS at `~/lionsos`.
- Microkit build contract: `MICROKIT_SDK` + `MICROKIT_BOARD=qemu_virt_aarch64`
  + `MICROKIT_CONFIG=debug`, `make` from an example dir.
- The build env vars + `aarch64-none-elf` PATH are **now persisted** to
  `~/.bashrc` (interactive login resolves `aarch64-none-elf-gcc`,
  `MICROKIT_SDK`, `LIONSOS`) — a fresh login shell can build without
  re-exporting. Fixed 2026-08-22 during the foundation audit.
