# Day-12 Decay Entry - 2026-09-08T16:01-16:26Z (t + ~216h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Fifth post-close confirmation row. Every batch verdict is
byte-identical to Day-11 (and Days 6-10): no resolver moved since the Day-5
Cloudflare event. Cloudflare's alg-18 validation held its eighth consecutive
day, now confirmed from three independent vantages this run (Paris probe,
instrument box, home line). **Apparatus change, labeled (caveat 1): the
`:5300` local validator is DOWN -- the Day-12 validator slot ran instead as
an authoritative-side serial check (receipt in hand, not a claim).**

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via Paris dig | serial 2026083011 (16:21Z; unchanged since Day-0) | serial 2026083103 (16:21Z; unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR, no EDE** (`flags: qr rd ra ad`, 16:21:59Z) -- **flip persisted >=184h (day 8)** | **AD=True** (`flags: qr rd ra ad`, 16:21:59Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:21:59Z) | AD=False, NOERROR, silent (same) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:21:59Z) | AD=False, NOERROR, silent (same) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, 16:25:31Z) | **AD=True** (16:25:31Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.170.246.157, 16:24:56Z) | **AD=True** (16:24:56Z) |

Controls: nlnetlabs.nl @1.1.1.1 from Paris AD=True (ANSWER:2 A+RRSIG,
RRSIG alg 8). Serials 2026083011 / 2026083103 byte-verified unchanged since
Day-0 / Day-2; RRSIG alg **18** (ML-DSA-44) present on both windows in the
Paris and home receipts. First Cloudflare AD 2026-09-01T16:03:38Z ->
still AD=True 2026-09-08T16:21:59Z.

### Batch verdicts -- specimen battery, 8/8 rows landed (batch
`b_1788883283_2b0a838ba607`, HTTP 202, queued 8/8 failed 0, rows
16:02:00-16:03:33Z; ad_consensus per domain from
`full_results.dnssec_analysis`; app_version 26.51.0-144-g385ba8783 --
**same binary as Days 6-11**)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (4/5, 1 unmeasured) | Unconfirmed |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |
| pqns.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-11 -> Day-12): pq = all five `ad_absent`
(identical). pq2 = **Quad9 `ad_absent` -> `unmeasured`** (the flaky slot
RECURRING; other four identical). pq-dualds = DNS4EU/OpenDNS/Quad9
`secure` + Google/Cloudflare `ad_absent` -- **Quad9 `ad_absent` (Day-11)
-> `secure`** (restored to its Day-6-9 shape). nlnetlabs.nl = all five
`secure`. The series' single resolver transition remains the Day-5
Cloudflare event.

### Day-12 verdict

**The Day-5 event held an eighth consecutive day.** Cloudflare's alg-18
validation persisted day 8 (first AD 2026-09-01T16:03:38Z -> still AD=True
2026-09-08T16:21:59Z), confirmed this run from Paris, the instrument box,
and the home line. Google, Quad9, OpenDNS, DNS4EU did not move; the
series' single transition remains bounded to
(2026-08-31T16:03Z, 2026-09-01T16:03:38Z]. Batch verdicts byte-identical
to Days 6-11 -- stability IS the finding. Authoritative serials unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator DOWN -- validator slot substituted, root
cause measured.** First dig attempt (16:13Z) timed out: nothing listens on
`127.0.0.1:5300` on the Mac (lsof empty), no pdns/unbound/knot/nsd process
or launchd unit anywhere, and no `:5300` listener on dnstool-app, lab-pc,
Paris, or either AWS measure box. Root cause found in Docker: ALL FOUR
pdns_recursor meson build containers
(flamboyant_sutherland/mystifying_ride/kind_bell/heuristic_yonath) exited 1
on 2026-08-30T02:2xZ ("MESON BUILD FAILED") -- the validator was never
running for Days 3-11 either. Honest reading: the Day-3..11 "validator"
rows assert a vantage whose live process has not existed since Aug 30;
their receipts live in prior-day docs and are NOT re-verified here (a
validator-down day could not have produced those receipts from :5300 --
treat prior-day validator rows as suspect until reconciled). Day-12
substitutes the authoritative-side serial check (serials in the Paris
receipt, byte-stable since Day-0/Day-2) and flags **validator repair as a
follow-up task -- it is the series' only remaining LOCAL validating
vantage**.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 7.** Batch
rows (16:02:00-16:03:33Z) again voted Cloudflare `ad_absent` on both
alg-18 windows while direct dig @1.1.1.1 from the same box (16:25Z),
Paris (16:21Z), and home (16:24Z) all read AD=True. Analyzer-side, not
the wire -- now a 7-day-replicated instrument defect, stable across the
same binary (-144) on Days 6-12.
3. **Quad9 flaky-slot pattern RECURRED (non-recurrence of the
non-recurrence).** Day-11 declared the 3-day flaky pattern broken; today
pq2's Quad9 batch vote is `unmeasured` again (4/5 completion), while
pq-dualds Quad9 answered `secure` and the Paris direct dig to 9.9.9.9
answered cleanly. Per Day-10's pre-registration this re-arms the
batch-runner Quad9 timeout review watch (low priority). Verdict-table
impact: pq2 ad_consensus reads "ad_absent (4/5, 1 unmeasured)" -- the
consensus WORD is unchanged; the completion count is the only delta.
4. **Row-verification keyed on `scan_source`** (value
`batch:<batch_id>`), not `_request_source` (carried from Day-11;
`_request_source` -> 42703); `ad_consensus`/`dnssec_state`/
`chain_of_trust` live inside `full_results->dnssec_analysis`, not as
columns. 8/8 rows found for this batch_id (ids 18740-18747).
5. **Egress-labeled receipts; one mislabel caught and relabeled.** The
first "app box" dig (16:24:56Z) was actually executed on the HOME line
(egress 97.170.246.157 in the receipt) -- caught by reading the egress
line before writing this doc, file relabeled /tmp/day12-home.txt, and the
real instrument-box digs re-run via ssh (egress 44.226.60.249,
16:25:31Z). Same lesson as Day-11 caveat 3, now a twice-replicated
protocol requirement: every vantage receipt carries its egress line, and
remote digs run ON the host.
6. **dig flag-order gotcha (first occurrence in series).** `+comments`
BEFORE `+noall` suppresses the `;; flags:` header line -- the run-1 Paris
receipt (16:21:15Z) carried serials but NO AD evidence. Correct order
`+noall +comments` (run-2, 16:21:59Z) restored the flags lines. A receipt
missing the flags line is an incomplete measurement, not an absence of AD.

Receipts: `/tmp/day12-paris.txt` (run-1, full RRSIG bodies + serials),
`/tmp/day12-paris-flags.txt` (run-2, header/flags lines), 
`/tmp/day12-appbox.txt` (instrument box, egress 44.226.60.249),
`/tmp/day12-home.txt` (home line, egress 97.170.246.157, relabeled),
`/tmp/day12-5300.txt` (timeout evidence, validator down). DB row dump +
per-resolver votes in the session record, batch
`b_1788883283_2b0a838ba607`. 2026-09-08T16:01-16:26Z.
Paris egress this run: 76.13.61.227 (IPv4; same path as Days 7-11).

-- hermes lane, 2026-09-08T16:30Z
