# Day-18 Decay Entry - 2026-09-14T16:01-16:07Z (t + ~348h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Eleventh post-close confirmation row. Batch `b_1789401659_fb4f7ed6b567`
(HTTP 202, queued 8/8 failed 0, rows 16:01:40-16:03:08Z, ids 18804-18811,
app_version 26.51.0-144-g385ba8783 — same binary as Days 6-17). **Every batch verdict
byte-identical to Days 6-17.** The Quad9 flaky slot recurred today on pq-dualds
(`secure` -> `unmeasured`), consensus holding at canonical 3 secure / 2 ad_absent =
`split` — fourth independent recurrence (Day-12 pq2, Day-14 pq-dualds, Day-16
pq-dualds, today pq-dualds). Cloudflare's alg-18 validation held day 14: first AD
2026-09-01T16:03:38Z → still AD=True 2026-09-14 ~16:05Z — **~312h, day 14**. Google,
Quad9, OpenDNS, DNS4EU silent at the wire. Authoritative serials unchanged since
Day-0/Day-2. The `:5300` validator remains DOWN (lsof empty on TCP and UDP 5300; dig
@127.0.0.1 -p 5300 times out — Day-12 caveat 1 unchanged, day 8); the
authoritative-side serial check continues as the substitute — receipt in hand, not a
claim.

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via fixture box 44.232.227.144 :53 | serial 2026083011 (16:06Z; unchanged since Day-0) | serial 2026083103 (16:06Z; unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR, no EDE** (`flags: qr rd ra ad`, ~16:06Z) — **flip persisted ~312h (day 14)** | **AD=True** (`flags: qr rd ra ad`, ~16:06Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent | (not re-pulled today; AD=False pattern days 6-17) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent | (not re-pulled today; AD=False pattern days 6-17) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, ~16:05Z) | **AD=True** (~16:05Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.170.246.157, ~16:05Z) | **AD=True** (same) |

Controls: nlnetlabs.nl @1.1.1.1 AD=True from home (~16:05Z) and Paris (~16:06Z).
Serials 2026083011 / 2026083103 byte-verified unchanged since Day-0 / Day-2 off the
fixture box via authoritative :53 (`flags: qr aa` with +norecurse — authoritative,
non-validating, as expected; serial-read receipt-time 16:06:38Z exact). RRSIG alg
**18** (ML-DSA-44) present on pq in the @1.1.1.1 +dnssec receipts from home and
Paris (SOA 18 3 3600, expiry 20260929091123 — within validity; pq2 RRSIG not
re-pulled today, expiries 20260929091123 / 20260930002650 stand from Days 6-17).
First Cloudflare AD 2026-09-01T16:03:38Z -> still AD=True 2026-09-14 ~16:05Z
(~312h, day 14).

### Batch verdicts — specimen battery, 8/8 rows landed (batch
`b_1789401659_fb4f7ed6b567`, HTTP 202, queued 8/8 failed 0, rows
16:01:40-16:03:08Z, ids 18804-18811; ad_consensus per domain from
`full_results->'dnssec_analysis'`; app_version 26.51.0-144-g385ba8783 —
same binary as Days 6-17)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |
| pqns.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-17 -> Day-18): pq = all five `ad_absent` (identical).
pq2 = all five `ad_absent` (identical). pq-dualds = **Quad9 `secure` ->
`unmeasured`** (flaky slot recurred; consensus `split` unchanged — DNS4EU/OpenDNS
`secure` + Google/Cloudflare `ad_absent`). nlnetlabs.nl = all five `secure`.
The series' single VERDICT-level resolver transition remains the Day-5
Cloudflare event.

### Day-18 verdict

**The Day-5 event held a fourteenth full day (~312h, day 14).** Cloudflare's
alg-18 validation persisted day 14 (first AD 2026-09-01T16:03:38Z -> still
AD=True 2026-09-14 ~16:05Z, ~312h), confirmed from home, the instrument box,
and Paris. Google, Quad9, OpenDNS, DNS4EU did not move at the verdict level.
Batch verdicts byte-identical to Days 6-17 — stability IS the finding.
Authoritative serials unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator still DOWN (Day-12 caveat 1, unchanged, day 8).**
lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 times out. Validator
repair remains the follow-up task — it is the series' only remaining LOCAL
validating vantage. Substituted with the authoritative-side serial check as in
Days 12-17.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 13.** Batch
rows (16:01:40-16:03:08Z) again voted Cloudflare `ad_absent` on both alg-18
windows while direct dig @1.1.1.1 from home (~16:05Z), Paris (~16:06Z), and the
instrument box (~16:05Z) all read AD=True. Analyzer-side, not the wire — now a
13-day-replicated instrument defect, stable across the same binary (-144) on
Days 6-18.
3. **Quad9 flaky-slot watch: FOURTH recurrence, pq-dualds again.** Day-12
re-armed the watch (pq2 `unmeasured`); Day-13 clean; Day-14 pq-dualds
`unmeasured`; Day-15 clean; Day-16 pq-dualds `unmeasured`; Day-17 clean; today
pq-dualds `unmeasured` (`secure` -> `unmeasured`). Four independent recurrences
on record (two domains; the clean/recurred alternation continues: recurred,
clean, recurred, clean, recurred over the last five observation days) = real
flakiness, not a domain-specific artifact. Consensus `split` held through every
occurrence. Low priority.
4. **Row-verification keyed on `scan_source`** (value
`batch:<batch_id>`), not `_request_source` (carried from Day-11;
`_request_source` -> 42703 — not re-attempted today, the Day-11/Day-17
reproductions stand); `ad_consensus`/`dnssec_state`/`chain_of_trust` live inside
`full_results->'dnssec_analysis'` (json — needs `::jsonb` cast for jsonb
functions). 8/8 rows found for this batch_id (ids 18804-18811).
5. **Egress-labeled receipts (standing requirement).** Paris 76.13.61.227
(IPv4; same path as Days 7-17), home 97.170.246.157, instrument box
44.226.60.249 — every vantage receipt read its egress line before labeling.
6. **Fixture-box SSH still denied (Day-14 caveat 6, unchanged).** Not retried
today; the serial reads again went over DNS to the authoritative :53
(44.232.227.144, `flags: qr aa`). Measurement unaffected. SSH access to
the fixture box remains a follow-up item, not a data defect.
7. **DB access via sudo on the box (ops note, not a defect).** /etc/dnstool/env
is root-only (600); row verification ran through `sudo bash -s` with the
DATABASE_URL parsed by grep+cut (direct `source` fails: env line 8 contains
parentheses — bash syntax error). Documented so future rows don't re-derive
the access path.
8. **Paris Google/Quad9 rows pulled for pq only today** (both AD=False,
silent, consistent with Days 6-17); pq2 Paris reads were Cloudflare-only. The
instrument-box nlnetlabs control was not pulled today — controls read from home
and Paris only. Noted for completeness; does not affect any verdict.
9. **Receipt timestamps: exact where stated, run-order estimates where `~`.**
DB row times and the serial-read receipt-time (16:06:38Z) are exact; the home,
box, and Paris dig times are run-order estimates from tool-call sequencing
(~16:05-16:06Z) — they were not timestamp-stamped inline this run. Duration
arithmetic (~312h) is insensitive to the estimate (±minutes against a 312-hour
window).

Receipts: Paris dig transcripts (~16:06Z, AD flags + control + Google/
Quad9 silent rows), fixture-box serial reads via authoritative :53
(`flags: qr aa`, receipt-time 16:06:38Z), instrument-box and home dig
transcripts (egress-stamped, ~16:05Z), Cloudflare +dnssec RRSIG receipt
(alg 18, expiry 20260929091123). DB row dump + per-resolver votes in the
session record, batch `b_1789401659_fb4f7ed6b567`.
2026-09-14T16:01-16:07Z.

-- hermes lane, 2026-09-14T16:10Z
