# Day-29 Decay Entry - 2026-09-25T16:01-16:06Z (t + ~576h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Twenty-second post-close confirmation row. Batch `b_1790352082_3c3404f80baa`
(HTTP 202 at 16:01:22Z — batch-id epoch; curl window opened 16:01:22Z, queued 8/8 failed 0, rows
16:02:00-16:03:32Z, ids 18925-18932, app_version 26.51.0-144-g385ba8783 — same binary as Days
6-28). **Every batch verdict tuple byte-identical to the Days 6-28 canonical state**
(dnssec_state / chain_of_trust / ad_consensus / display_label, all 8 domains) — but the
**within-split flaky slot MOVED**: a Quad9 `unmeasured` variant fired on **pq2** (3rd pq2
recurrence, 7th on record) while **pq-dualds restored to canonical membership** (Quad9 `secure`,
the one-day pattern held exactly as watched yesterday); both consensus tuples unchanged
(`ad_absent` / `split`). Cloudflare's alg-18 validation held day 25: first AD
2026-09-01T16:03:38Z -> still AD=True at vantage receipts 16:01:57Z (home, RRSIG pull),
16:02:19Z (instrument box), 16:05:39Z (Paris) — **~576h, day 25** (576.0h exactly at the
16:03:38Z anchor; 24 days x 24h; home + box receipts land just before the anchor's time-of-day,
Paris after). Google, Quad9, OpenDNS, DNS4EU silent at the wire (Paris AD=False rows on both
windows; controls AD=True on all three resolvers). One **labeled transient**: home @1.1.1.1
returned a single SERVFAIL on pq at 16:02:16Z between a successful RRSIG pull and 5/5 immediate
NOERROR+ad retries (see caveat 6 — classified a one-off Cloudflare edge flake, NOT a
verdict-level transition). Authoritative serials unchanged since Day-0/Day-2. The `:5300`
validator remains DOWN (lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 -> "no servers
could be reached" — Day-12 caveat 1 unchanged, day 19, probed over the Day-25-restored
fixture-box SSH path); the authoritative-side serial check continues as the substitute —
receipt in hand, not a claim.

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via fixture box 44.232.227.144 :53 | serial 2026083011 (read 16:04:25Z; unchanged since Day-0) | serial 2026083103 (unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR** (`flags: qr rd ra ad`, 16:05:39Z) — **flip persisted ~576h (day 25)** | **AD=True** (`flags: qr rd ra ad`, 16:05:39Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:05:39Z) | AD=False, NOERROR, silent (16:05:39Z) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent (`flags: qr rd ra`, 16:05:39Z) | AD=False, NOERROR, silent (16:05:39Z) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, 16:02:19Z) | **AD=True** (16:02:19Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.218.188.26; 5/5 probes 16:02:20-16:03:06Z; one transient SERVFAIL at 16:02:16Z — caveat 6) | **AD=True** (16:02:17Z) |

Controls: nlnetlabs.nl @1.1.1.1 AD=True from home (16:02:17Z), the instrument box (16:02:19Z),
and Paris (16:05:39Z); Paris controls on 8.8.8.8 and 9.9.9.9 also AD=True (16:05:39Z).
Serials 2026083011 / 2026083103 byte-verified unchanged since Day-0 / Day-2 off the fixture box
via authoritative :53 (`flags: qr aa` with +norecurse — authoritative, non-validating, as
expected). **Both RRSIGs re-pulled today** from home (@1.1.1.1 +dnssec, 16:01:57Z): pq SOA RRSIG
alg **18** (ML-DSA-44), key tag 33846, inception 20260830081123, expiry 20260929091123; pq2
RRSIG alg 18, key tag 46383, inception 20260830232650, expiry 20260930002650 — both within
validity, key fields matching the Days 6-28 receipts byte-for-byte. First Cloudflare AD
2026-09-01T16:03:38Z -> still AD=True 2026-09-25 16:01-16:06Z (~576h, day 25).

### Batch verdicts — specimen battery, 8/8 rows landed (batch
`b_1790352082_3c3404f80baa`, HTTP 202 at 16:01:22Z, queued 8/8 failed 0, rows
16:02:00-16:03:32Z, ids 18925-18932; ad_consensus per domain from
`full_results->'dnssec_analysis'`; app_version 26.51.0-144-g385ba8783 — same
binary as Days 6-28)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (4/5 resolvers + Quad9 `unmeasured`) | Unconfirmed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| pqns.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-28 -> Day-29): **pq = all five `ad_absent` (identical);
pq2 = Cloudflare/Google/OpenDNS/DNS4EU `ad_absent` + **Quad9 `unmeasured`**
— the flaky slot fired on pq2 (4/5 `ad_absent` keeps the consensus tuple
`ad_absent` unchanged; same disposition as the Day-12/22 pq2 variants);
pq-dualds = Cloudflare/Google `ad_absent` + DNS4EU/OpenDNS/Quad9 `secure` —
**canonical membership restored after one day**, exactly per the established
pattern (Day-28's watch item resolved as predicted); nlnetlabs.nl = all five
`secure` (identical).** The series' single VERDICT-level resolver transition
remains the Day-5 Cloudflare event.

### Day-29 verdict

**The Day-5 event held a twenty-fifth full day (~576h, day 25).** Cloudflare's
alg-18 validation persisted day 25 (first AD 2026-09-01T16:03:38Z -> still
AD=True 2026-09-25 16:01-16:06Z, ~576h — 576.0h exactly at the anchor, 24 days
x 24h), confirmed from home, the instrument box, and Paris. Google, Quad9,
OpenDNS, DNS4EU did not move at the verdict level. Batch verdict tuples
byte-identical to the Days 6-28 canonical state — stability IS the finding —
with the within-split flaky slot MOVED from pq-dualds (restored to canonical
`secure`, one-day pattern held) to pq2 (Quad9 `unmeasured`, 7th recurrence on
record; both consensus tuples unchanged). One labeled transient at home (single
Cloudflare edge SERVFAIL, canonical AD=True — caveat 6). Authoritative serials
unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator still DOWN (Day-12 caveat 1, unchanged, day 19).**
lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 -> "no servers could be
reached" (probed on-box 16:02:19Z). Validator repair remains the follow-up task —
the series' only remaining LOCAL validating vantage. Substituted with the
authoritative-side serial check as in Days 12-28. The probe ran over the
Day-25-restored fixture-box SSH path, which worked first try again.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 24.** Batch rows
(16:02:00-16:03:32Z) again voted Cloudflare `ad_absent` on both alg-18 windows
while direct dig @1.1.1.1 from home (16:02:20-16:03:06Z), the instrument box
(16:02:19Z), and Paris (16:05:39Z) all read AD=True. Analyzer-side, not the wire
— now a 24-day-replicated instrument defect, stable across the same binary
(-144) on Days 6-29.
3. **Quad9 flaky-slot watch: MOVED SLOTS — pq-dualds restored, pq2 fired.**
Day-12 pq2 `unmeasured`; Day-13 clean; Day-14 pq-dualds; Day-15 clean; Day-16
pq-dualds; Day-17 clean; Day-18 pq-dualds; Day-19 clean; Day-20 clean; Day-21
clean; Day-22 pq2 `unmeasured`; Day-23 clean; Day-24 clean; Day-25 clean; Day-26
clean; Day-27 clean; Day-28 pq-dualds `unmeasured`; Day-29 **pq2 `unmeasured`**.
Recurrences on record now SEVEN (pq2 Day-12/22/29, pq-dualds Day-14/16/18/28).
Consequence today: pq-dualds canonical membership (Quad9 `secure`) RESTORED
after one day — the Day-28 watch item resolved exactly per the one-day pattern,
no escalation; pq2's Quad9 vote went `unmeasured` for the first time since
Day-22, consensus tuple `ad_absent` unchanged (4/5). Escalation rule now applies
to the pq2 slot: if Quad9 stays `unmeasured` there a second day, that escalates
pq2 from flaky to suspect.
4. **Row-verification keyed two ways, both agree (8/8).** Primary:
`scan_source = 'batch:b_1790352082_3c3404f80baa'` (8 rows). Cross-check:
`full_results->>'_request_source' = 'batch'` (text compare, 8 rows; the
15-minute-window form from the Day-0 tasking works only as this text compare —
the `_request_source` TABLE COLUMN does not exist, 42703, carried from Day-11;
the jsonb-coercion form `(full_results::jsonb->'_request_source') = 'batch'`
throws `invalid input syntax for type json` (Day-25); `||` and `->` share one
precedence level — parenthesize json paths (Day-25). `ad_consensus`/
`dnssec_state`/`chain_of_trust`/`display_label` live inside
`full_results->'dnssec_analysis'` (json). Inherited rows (dane*, pqns) carry NO
ad_consensus/resolver_ad keys at all — "-" in the table is key-absence, not an
empty vote set.
5. **Mid-flight row-count trap: did NOT fire this run.** The single count fired
at 16:04:27Z — 3m05s after the 202, AFTER the staggered-write window fully
closed (rows span 16:02:00.4 -> 16:03:32.3Z, ~92s) — and returned 8/8
immediately. The Day-21 standing guidance (count >= 3 min after the 202)
continues to hold; no mid-flight read occurred today because the count was
scheduled past the observed ~92s write span, not because the trap is gone.
6. **NEW labeled transient: single home SERVFAIL (one-off Cloudflare edge
flake).** dig @1.1.1.1 pq SOA from home returned **SERVFAIL once at 16:02:16Z**
(empty answer, `flags: qr rd ra`) — between a successful full RRSIG pull at
16:01:57Z (NOERROR, alg-18 RRSIG in hand) and 5/5 immediate retries
16:02:20-16:03:06Z (NOERROR + `ad` on BOTH fixtures, same query, same resolver,
same minute). Classification: single transient at a Cloudflare anycast edge —
NOT a verdict-level transition, NOT counted as a resolver vote change; the
canonical home reading is AD=True on both fixtures. pq2 was clean in the same
window (16:02:17Z, AD=True), and the instrument box (16:02:19Z) and Paris
(16:05:39Z) both read AD=True on pq — three independent confirmations the zone
and its signature were healthy throughout. No prior-day analog in the series
receipts (Days 6-28 home receipts all NOERROR); logged here so a future REAL
Cloudflare flip cannot hide behind "we saw that once before," and so the one-day
SERVFAIL observation is on the record with its resolution. Watch: if a transient
SERVFAIL recurs on the alg-18 windows from the same home vantage, start a
transient counter — a second occurrence pattern would deserve its own watch
line, distinct from the verdict-level Day-5 event class.
7. **Egress-labeled receipts (standing requirement) — no rotation this run.**
Paris 76.13.61.227 (IPv4; same path as Days 7-28), instrument box 44.226.60.249,
home 97.218.188.26 (same as Days 25-28; every vantage receipt read its egress line
before labeling, `curl -4` throughout; dual-stack vantages, IPv4 labels kept).
8. **SG port-22 allowlists: no repair needed this run (ops note, not a data
defect).** Home egress stable at 97.218.188.26 (Days 25-28); dnstool-app SSH
verified first try (16:02:19Z); fixture-box SSH verified first try from home via
the dnstool-app-2026.pem (16:04:25Z). Path clarification carried from Day-27: the
dnstool-app box itself carries only `authorized_keys` in ~/.ssh — no pem there;
the fixture-box path is home-side with ~/.ssh/dnstool-app-2026.pem. Posture
unchanged on both boxes: port 22 from the current public IP only. Standing
consequence unchanged: if the home egress rotates again, SSH breaks the same
way; the SG refresh recipe is documented at Day-23 caveat 7.
9. **DB access via sudo on the box (ops note, not a defect).** /etc/dnstool/env
is root-only (600); row verification ran through `sudo -n bash -s` with the
DATABASE_URL parsed by grep+cut (direct `source` fails: env line contains
parentheses — bash syntax error; carried from Day-24 caveat 9).
10. **Forward watch (carried, updated countdown): pq RRSIG expiry approaches.**
pq SOA RRSIG expires 2026-09-29T09:11:23Z (**4 days out**; pq2 expires
2026-09-30T00:26:50Z, **5 days out**). Both within validity today (receipts
re-pulled at 16:01:57Z, key fields byte-matching the Days 6-28 values); if the
series is still running at expiry, the windows need a re-sign or the rows will
show the expiry honestly. Carried so the transition is not misread as a decay
event.

Receipts: Paris dig transcripts (16:05:39Z, AD flags on pq + pq2 + controls on
all three resolvers), fixture-box serial reads via authoritative :53 (`flags: qr
aa`, 16:04:25Z), instrument-box and home dig transcripts (egress-stamped,
16:01:57-16:05:39Z), Cloudflare +dnssec RRSIG receipts for BOTH fixtures (alg 18;
key tags 33846 / 46383; expiries 20260929091123 / 20260930002650), on-box :5300
lsof+dig probe transcript (16:02:19Z). DB row dump + per-resolver votes in the
session record, batch `b_1790352082_3c3404f80baa`. 2026-09-25T16:01-16:06Z.

-- hermes lane, 2026-09-25T16:12Z
