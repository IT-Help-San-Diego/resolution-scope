# Day-21 Decay Entry - 2026-09-17T16:02-16:07Z (t + ~384h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Fourteenth post-close confirmation row. Batch `b_1789660921_82d70ba6ef79`
(HTTP 202, queued 8/8 failed 0, rows 16:02:37-16:04:11Z, ids 18836-18843,
app_version 26.51.0-144-g385ba8783 — same binary as Days 6-20). **Every batch verdict
byte-identical to the Days 6-20 canonical state.** The Quad9 flaky slot was CLEAN
today — third consecutive clean day (Days 19-21); pq-dualds again at canonical
3 secure / 2 ad_absent = `split` with Quad9 `secure`. The strict day-on/day-off
alternation (6-for-6 through Day 19) remains broken by the Days 19-21 clean run;
recurrences on record remain four (two domains), consensus `split` held through
every occurrence. Cloudflare's alg-18 validation held day 17: first AD
2026-09-01T16:03:38Z → still AD=True at vantage receipts ~16:02-16:07Z —
**~384h, day 17** (exactly 384.0h at the 16:03:38Z anchor; 16 days × 24h).
Google, Quad9, OpenDNS, DNS4EU silent at the wire. Authoritative serials
unchanged since Day-0/Day-2. The `:5300` validator remains DOWN (lsof empty on
TCP and UDP 5300; dig @127.0.0.1 -p 5300 times out — Day-12 caveat 1 unchanged,
day 11); the authoritative-side serial check continues as the substitute —
receipt in hand, not a claim.

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via fixture box 44.232.227.144 :53 | serial 2026083011 (~16:03Z; unchanged since Day-0) | serial 2026083103 (~16:03Z; unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR** (`flags: qr rd ra ad`, ~16:05Z) — **flip persisted ~384h (day 17)** | **AD=True** (`flags: qr rd ra ad`, ~16:05Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent (`flags: qr rd ra`, ~16:05Z) | (not re-pulled today; AD=False pattern days 6-20) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent (`flags: qr rd ra`, ~16:05Z) | (not re-pulled today; AD=False pattern days 6-20) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, ~16:05Z) | **AD=True** (~16:05Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.170.246.157, ~16:03Z) | **AD=True** (same) |

Controls: nlnetlabs.nl @1.1.1.1 AD=True from home (~16:03Z), Paris (~16:05Z),
and the instrument box (~16:05Z).
Serials 2026083011 / 2026083103 byte-verified unchanged since Day-0 / Day-2 off the
fixture box via authoritative :53 (`flags: qr aa` with +norecurse — authoritative,
non-validating, as expected). **Both RRSIGs re-pulled today** from home
(@1.1.1.1 +dnssec): pq SOA RRSIG alg **18** (ML-DSA-44), key tag 33846, inception
20260830081123, expiry 20260929091123; pq2 RRSIG alg 18, key tag 46383, inception
20260830232650, expiry 20260930002650 — both within validity, matching the
Days 6-20 receipts. First Cloudflare AD 2026-09-01T16:03:38Z -> still AD=True
2026-09-17 ~16:02-16:07Z (~384h, day 17).

### Batch verdicts — specimen battery, 8/8 rows landed (batch
`b_1789660921_82d70ba6ef79`, HTTP 202, queued 8/8 failed 0, rows
16:02:37-16:04:11Z, ids 18836-18843; ad_consensus per domain from
`full_results::jsonb->'dnssec_analysis'`; app_version 26.51.0-144-g385ba8783 —
same binary as Days 6-20)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |
| pqns.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-20 -> Day-21): pq = all five `ad_absent` (identical).
pq2 = all five `ad_absent` (identical). pq-dualds = Quad9 `secure` held
(flaky slot clean for the third consecutive day; canonical 3 secure /
2 ad_absent — Quad9/DNS4EU/OpenDNS `secure` + Google/Cloudflare `ad_absent`).
nlnetlabs.nl = all five `secure`. The series' single VERDICT-level resolver
transition remains the Day-5 Cloudflare event.

### Day-21 verdict

**The Day-5 event held a seventeenth full day (~384h, day 17).** Cloudflare's
alg-18 validation persisted day 17 (first AD 2026-09-01T16:03:38Z -> still
AD=True 2026-09-17 ~16:02-16:07Z, ~384h — exactly 384.0h at the 16:03:38Z
anchor, 16 days × 24h; receipts bracket the anchor), confirmed from home, the
instrument box, and Paris. Google, Quad9, OpenDNS, DNS4EU did not move at the
verdict level. Batch verdicts byte-identical to the Days 6-20 canonical state —
stability IS the finding. Authoritative serials unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator still DOWN (Day-12 caveat 1, unchanged, day 11).**
lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 times out. Validator
repair remains the follow-up task — it is the series' only remaining LOCAL
validating vantage. Substituted with the authoritative-side serial check as in
Days 12-20.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 16.** Batch
rows (16:02:37-16:04:11Z) again voted Cloudflare `ad_absent` on both alg-18
windows while direct dig @1.1.1.1 from home (~16:03Z), Paris (~16:05Z), and the
instrument box (~16:05Z) all read AD=True. Analyzer-side, not the wire — now a
16-day-replicated instrument defect, stable across the same binary (-144) on
Days 6-21.
3. **Quad9 flaky-slot watch: CLEAN — third consecutive clean day; the
alternation stays broken.** Day-12 recurred (pq2 `unmeasured`); Day-13 clean;
Day-14 pq-dualds; Day-15 clean; Day-16 pq-dualds; Day-17 clean; Day-18
pq-dualds; Day-19 clean; Day-20 clean; Day-21 clean. Recurrences on record
remain four (two domains), consensus `split` held through every occurrence.
Low priority — recorded because the pattern's shape changed at Day 20, not
because a verdict moved.
4. **Row-verification keyed on `scan_source`** (value `batch:<batch_id>`),
not `_request_source` (carried from Day-11; `_request_source` -> 42703 — not
re-attempted today, the Day-11/17/19 reproductions stand);
`ad_consensus`/`dnssec_state`/`chain_of_trust` live inside
`full_results->'dnssec_analysis'` (json — `::jsonb` cast used).
Clarification of Day-20 caveat 4's phrasing: `display_label` is a KEY inside
that JSON, not a table column (information_schema shows no `%label%` column on
domain_analyses — checked today); the labels in the table above are read from
the JSON, not derived. 8/8 rows found for this batch_id (ids 18836-18843); the
15-minute-window cross-check (`scan_source LIKE 'batch:%'`) returned 8 after
completion.
5. **Mid-flight row-count trap (NEW ops note, not a data defect).** The first
15-minute cross-check, run ~2.5 min after the HTTP 202, returned 5 of 8 — the
battery writes rows staggered over ~95s (16:02:37 -> 16:04:11Z), so a
verification pass fired too early reads as missing rows. A re-check ~1 min
later returned 8/8; all verdict reads in this row are post-completion.
Standing guidance: wait ~3 min after the 202 before the first row count.
6. **Egress-labeled receipts (standing requirement).** Paris 76.13.61.227
(IPv4; same path as Days 7-20), home 97.170.246.157, instrument box
44.226.60.249 — every vantage receipt read its egress line before labeling
(`curl -4` used throughout; dual-stack vantages, IPv4 labels kept).
7. **Fixture-box SSH still denied (Day-14 caveat 6, unchanged).** Not retried
today; the serial reads again went over DNS to the authoritative :53
(44.232.227.144, `flags: qr aa`). Measurement unaffected. SSH access to
the fixture box remains a follow-up item, not a data defect.
8. **DB access via sudo on the box (ops note, not a defect).** /etc/dnstool/env
is root-only (600); row verification ran through `sudo bash -s` with the
DATABASE_URL parsed by grep+cut (direct `source` fails: env line 8 contains
parentheses — bash syntax error). Documented so future rows don't re-derive
the access path.
9. **Paris Google/Quad9 rows pulled for pq only today** (both AD=False,
silent, consistent with Days 6-20); pq2 Paris reads were Cloudflare-only.
The instrument-box nlnetlabs control WAS pulled today (AD=True). Noted for
completeness; does not affect any verdict.
10. **Receipt timestamps: exact where stated, run-order estimates where `~`.**
DB row times and the batch fire time (16:02:00-16:02:01Z) are exact; the home,
box, Paris, and serial-read dig times are run-order estimates from tool-call
sequencing (~16:02-16:07Z) — they were not timestamp-stamped inline this run.
Duration arithmetic (~384h) is insensitive to the estimate (±minutes against a
384-hour window; the 384.0h figure is computed from the exact Day-0 anchor at
16:03:38Z).

Receipts: Paris dig transcripts (~16:05Z, AD flags on pq + pq2 + control +
Google/Quad9 silent rows), fixture-box serial reads via authoritative :53
(`flags: qr aa`), instrument-box and home dig transcripts (egress-stamped,
~16:03-16:05Z), Cloudflare +dnssec RRSIG receipts for BOTH fixtures (alg 18;
key tags 33846 / 46383; expiries 20260929091123 / 20260930002650). DB row
dump + per-resolver votes in the session record, batch
`b_1789660921_82d70ba6ef79`. 2026-09-17T16:02-16:07Z.

-- hermes lane, 2026-09-17T16:11Z
