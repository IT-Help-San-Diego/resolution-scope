# Day-14 Decay Entry - 2026-09-10T16:01-16:12Z (t + ~264h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Seventh post-close confirmation row. Every batch verdict is
byte-identical to Days 6-13: no resolver moved since the Day-5 Cloudflare
event. Cloudflare's alg-18 validation held its tenth consecutive day,
confirmed again from three independent vantages (Paris probe, instrument box,
home line). The Quad9 flaky-slot MOVED DOMAINS today: pq and pq2 both
completed 5/5, but pq-dualds recorded Quad9 `unmeasured` (4/5) -- consensus
label unchanged (`split`). The `:5300` validator remains DOWN (lsof empty on
TCP and UDP 5300; dig @127.0.0.1 -p 5300 times out -- Day-12 caveat 1
unchanged); the authoritative-side serial check continues as the substitute
-- receipt in hand, not a claim.

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via fixture box 44.232.227.144 :53 | serial 2026083011 (16:10Z; unchanged since Day-0) | serial 2026083103 (16:10Z; unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR, no EDE** (`flags: qr rd ra ad`, 16:09:56Z) -- **flip persisted >=240h (day 10)** | **AD=True** (`flags: qr rd ra ad`, 16:09:56Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent | AD=False, NOERROR, silent (same) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent | AD=False, NOERROR, silent (retry at 16:10:58Z clean) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, 16:08Z) | **AD=True** (16:08Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.170.246.157, 16:08Z) | **AD=True** (16:08Z) |

Controls: nlnetlabs.nl @1.1.1.1 from Paris AD=True (16:09:56Z). Serials
2026083011 / 2026083103 byte-verified unchanged since Day-0 / Day-2 off the
fixture box (`flags: qr aa rd` -- authoritative, non-validating, as
expected). RRSIG alg **18** (ML-DSA-44) present on both windows in the
Cloudflare @1.1.1.1 +dnssec receipt (SOA 18 3 3600 ... expiry 20260929 --
within validity). First Cloudflare AD 2026-09-01T16:03:38Z -> still AD=True
2026-09-10T16:09:56Z.

### Batch verdicts -- specimen battery, 8/8 rows landed (batch
`b_1789056073_1f1bc40c8d89`, HTTP 202, queued 8/8 failed 0, rows
16:01:53-16:03:21Z, ids 18762-18769; ad_consensus per domain from
`full_results.dnssec_analysis`; app_version 26.51.0-144-g385ba8783 --
**same binary as Days 6-13**)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |
| pqns.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-13 -> Day-14): pq = all five `ad_absent`
(identical). pq2 = all five `ad_absent` (identical; Day-13 also 5/5).
pq-dualds = **Quad9 `secure` -> `unmeasured`** (Day-12's flaky slot moved
hosts; DNS4EU/OpenDNS `secure` + Google/Cloudflare `ad_absent`, 4/5 this
day). nlnetlabs.nl = all five `secure`. The series' single VERDICT-level
resolver transition remains the Day-5 Cloudflare event.

### Day-14 verdict

**The Day-5 event held a tenth consecutive day.** Cloudflare's alg-18
validation persisted day 10 (first AD 2026-09-01T16:03:38Z -> still AD=True
2026-09-10T16:09:56Z), confirmed from Paris, the instrument box, and the
home line. Google, Quad9, OpenDNS, DNS4EU did not move at the verdict
level. Batch verdicts byte-identical to Days 6-13 -- stability IS the
finding. Authoritative serials unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator still DOWN (Day-12 caveat 1, unchanged).**
lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 times out. Validator
repair remains the follow-up task -- it is the series' only remaining
LOCAL validating vantage. Substituted with the authoritative-side serial
check as in Days 12-13.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 9.** Batch
rows (16:01:53-16:03:21Z) again voted Cloudflare `ad_absent` on both
alg-18 windows while direct dig @1.1.1.1 from the same box (16:08Z),
Paris (16:09Z), and home (16:08Z) all read AD=True. Analyzer-side, not
the wire -- now a 9-day-replicated instrument defect, stable across the
same binary (-144) on Days 6-14.
3. **Quad9 flaky-slot watch: slot moved domains (new shape, watch stays
armed).** Day-12 re-armed the watch after Quad9 `unmeasured` on pq/pq2;
Day-13 was clean (5/5); today the slot answered cleanly on pq/pq2 but
produced `unmeasured` on pq-dualds. Consensus label `split` unchanged
(3 secure / 2 ad_absent -> 3 secure / 1 ad_absent / 1 unmeasured). Two
independent recurrences (different domains) = real flakiness, not a
domain-specific artifact. Low priority.
4. **Row-verification keyed on `scan_source`** (value
`batch:<batch_id>`), not `_request_source` (carried from Day-11;
`_request_source` -> 42703); `ad_consensus`/`dnssec_state`/
`chain_of_trust` live inside `full_results->'dnssec_analysis', not as
columns. 8/8 rows found for this batch_id (ids 18762-18769).
5. **Egress-labeled receipts (standing requirement).** Paris 76.13.61.227
(IPv4; same path as Days 7-13), home 97.170.246.157, instrument box
44.226.60.249 -- every vantage receipt read its egress line before
labeling.
6. **Fixture-box SSH denied (new, cosmetic).** ssh ubuntu@/root@
44.232.227.144 both denied (publickey) -- the serial reads went over DNS
to the authoritative :53 instead (44.232.227.144, `flags: qr aa rd`).
Measurement unaffected; noted because earlier entries read serials ON the
box. SSH access to the fixture box is a follow-up item, not a data defect.

Receipts: Paris dig transcripts (16:09:56Z, AD flags + control + Google/
Quad9 silent rows; Quad9/pq2 clean retry 16:10:58Z), fixture-box serial
reads via authoritative :53 (`flags: qr aa rd`, 16:10:46Z), instrument-box
and home dig transcripts (egress-stamped, 16:08Z), Cloudflare +dnssec RRSIG
receipt (alg 18, 16:10Z). DB row dump + per-resolver votes in the session
record, batch `b_1789056073_1f1bc40c8d89`. 2026-09-10T16:01-16:12Z.

-- hermes lane, 2026-09-10T16:14Z
