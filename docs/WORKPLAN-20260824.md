# WORKPLAN — Resolution Scope autonomous block (2026-08-24 08:00 PDT)

Self-contained direction for the agent while Carey is on a client call.
Everything here is either already ruled by Carey or safe to do without a ruling.
Items that NEED Carey or the team are in the BLOCKED section at the bottom —
do not spend a decision on them; surface them when he's back.

## Ordered queue (execute top to bottom, verify each)

1. **Version stamp → `v26.0.0-alpha.1` + git provenance.**
   - Add `engine/build.rs` that emits the git sha (`git describe --always --dirty`)
     into a `RESOLUTION_SCOPE_GIT_VERSION` env var, with a graceful fallback.
   - Change `engine_version()` (engine/src/seal.rs) to combine
     `CARGO_PKG_VERSION` + git sha, e.g. `26.0.0-alpha.1-abc1234`.
   - Bump `engine/Cargo.toml` `version` to `26.0.0-alpha.1`.
   - Load-bearing: this changes `produced_by_version` in the seal preimage. That
     is CORRECT and is the entire point (provenance). The native golden test
     pins `"0.1.0"` deliberately and stays untouched.
   - Verify: engine tests green, a real scan prints the new version string, and
     the store stamps it (record_scan calls engine_version()).

2. **TUI keycap row — physical ASCII keys.**
   - Replace `j/k ↑↓ · enter · esc …` help line with old-school Unix keycaps:
     `[1]` … `[q]`, `[enter]`, `[esc]`, `[tab]`, `[↑↓]` → ASCII only where a
     glyph would tofu. Pure presentation, cli/src/tui.rs only, no seal impact.

3. **Copy-tightening pass** (the "how short can this text be" goal).
   - Help line + tab labels + section subtitles. First pass; Carey reviews.

4. **Corpus measurements (informs the two open rulings):**
   - `+all` SPF prevalence — how often does `+all` appear in the wild?
   - CDS N2 rate — among CDS-publishing zones, how often does CDS ≠ parent DS?
   - Source: read-only against the DNS Tool corpus (domain_analyses) or a live
     sample scan. Never write to the old tool's DB.
   - Write results into docs/DEBRIEF-four-mind-20260824.md as data.

5. **Scan-motion (real data only).**
   - Flash a control's row only when its measured state changes; live
     "N of 8 measured" ticker. No fake spinner. First version.

6. **Re-check lane traffic** — pull origin, read LANES.md, fold in any Claude
   Code/Science sound-off on the debrief (esp. the minimal-4-state-icon).

## BLOCKED — do not build these; surface when Carey returns

- **Persist-by-default + `--discard`** — Carey ruled the semantics (persist
  always, delete only on explicit flag), but the DEFAULT store location is
  still open (debrief §3a). Don't pick a Postgres default unilaterally.
- **Minimal-4-state icon** — await the team's sound-off before building; may
  prototype a positional-grid spike but don't merge it as the answer.
- **Tab layout A vs B** — Carey said it's downstream of the copy-tightening;
  do the tightening (item 3) first, present the A/B choice after.
- **DNS Tool → Resolution Scope ingestion** — needs the `source_instrument`
  schema change + team sign-off on the lossy mapping. Not solo work.

## Standing rules for this block

- Verify each item at source before claiming done; run the gates (tests +
  clippy strict + citation boundary) after any .rs change.
- Commit to main only when green and pushed; check `git status` first (shared
  checkout, other lanes active).
- Call hindsight_recall before asserting any "we decided X" — do not lean on
  injected memory.
