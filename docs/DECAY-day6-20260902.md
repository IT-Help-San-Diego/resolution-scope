# Day-6 Decay Entry — 2026-09-02T16:01–16:13Z (t + ~72h)
## Both windows: :5300 + Paris + batch verdicts, per the Day-0 protocol

**Row 6 of 6, window 1 (per the Day-4/5 row counting) — first CONFIRMATION row
of the Day-5 transition.** The Cloudflare alg-18 flip persisted a full day; no
other resolver moved. Protocol bookkeeping note labeled in the verdict.

---

### Validator + public vantages (both windows)

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Local validator | `:5300` PowerDNS | **AD=True** (`flags: qr rd ra ad`, DNSKEY ANSWER:2, 16:09:24Z; serial unchanged) | **AD=True** (same receipt set, 16:09:24Z; serial unchanged) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR, no EDE-1** (16:11:09Z) — **flip persisted ≥24h** (24–48h given the Day-5 bracket) | **AD=True, no EDE-1** (16:11:09Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent (unchanged) | AD=False, silent (unchanged) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent (unchanged) | AD=False, silent (unchanged; one benign **EDE 29** "Result from negative cache" — provenance info, not an algorithm refusal) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (direct dig 16:09:39Z — see caveat 1) | **AD=True** (direct dig 16:09:39Z) |

**Quartet re-measure (per the 20260902 field report's registered next step):**
Google + Quad9 still silent AD=False from Paris at 16:11Z; OpenDNS + DNS4EU
still `ad_absent` in the batch's per-resolver votes. No second resolver has
crossed. Controls sane: nlnetlabs.nl @1.1.1.1 AD=True; +cdflag correctly
suppresses AD (`flags: qr rd ra cd`); pqns A + parent alg-13 RRSIG (keytag
6990) AD=True — inherited chain intact.

### Batch verdicts — specimen battery, 8/8 rows landed (batch `b_1788364876_1c4fc6b6a77b`, HTTP 202, queued 8/8 failed 0, rows 16:01:53–16:03:24Z; ad_consensus per domain, from `full_results.dnssec_analysis`; app_version 26.51.0-144-g385ba8783 — **binary moved** vs Day-5's 26.51.0-140-g812c18080; the 4 intervening commits touch release/docs, batch key-charging, dnsclient glue reads, and a usage string — `git diff --stat 812c18080..385ba8783 -- go-server/internal/analyzer go-server/internal/dbq` is EMPTY)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label | alg |
|---|---|---|---|---|---|
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed | 8 |
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed | 18 |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (4 ad_absent + Quad9 unmeasured) | Unconfirmed | 18 |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed | **18** (was 8 — caveat 2) |
| dane.resolutionscope.com | present | inherited | — | Inherited | 13 |
| mx.dane.resolutionscope.com | present | inherited | — | Inherited | 13 |
| dane-rx.resolutionscope.com | present | inherited | — | Inherited | 13 |
| pqns.resolutionscope.com | present | inherited | — | Inherited | 13 |

### Day-6 verdict (row 6 of 6, window 1; row 6, window-2 comparison)

**The Day-5 event held.** Cloudflare's alg-18 validation persisted ≥24h
(first AD 2026-09-01T16:03:38Z → still AD=True at 16:09–16:11Z on
2026-09-02), same shape as Day-5 (AD on A/NODATA/DS/DNSKEY, no EDE-1 anywhere
at Cloudflare). Google, Quad9, OpenDNS, DNS4EU did not move — the series'
single transition remains bounded to (2026-08-31T16:03Z, 2026-09-01T16:03:38Z].
For the four unchanged behaviors, Day-6 is a stability row: stability IS the
finding. Both windows byte-stable at :5300 (serials 2026083011 / 2026083103
unchanged since Day-0 / Day-2).

**Labeled caveats (verification discipline):**

1. **Instrument-vs-wire Cloudflare discrepancy replicates — Day-5's
   pre-registered discriminating outcome is in.** Batch rows (16:02:18–23Z)
   again voted Cloudflare `ad_absent` on both alg-18 windows, while direct
   dig @1.1.1.1 from the same box (dnstool-app) read AD=True 6–7 minutes
   later (16:09:39Z), and Paris read AD=True at 16:11:09Z. Per Day-5's
   registered test ("if it still reads ad_absent, the discrepancy lives in
   the analyzer, not the wire"): **analyzer-side, not the wire.** Now a
   2-day-replicated instrument defect, and the cache-horizon hypothesis is
   dead — the wire was AD=True from the batch box's own vantage minutes
   after the rows landed.
2. **pq-dualds alg field 8→18 is specimen-side, not binary-side.** Wire
   (16:09:39Z): dual DS — `911 18 2` + `40759 8 2` — dual DNSKEY
   (`257 3 8` + `257 3 18`), dual RRSIG DNSKEY (keytags 911 + 40759, both
   inception 2026-08-31T00:54:46Z). The B5 dual-DS upsert (its DRAFT doc
   recorded it not-executed on 2026-08-30) is live at the delegation; with
   the analyzer diff between the two binaries empty, the field flip tracks
   the delegation change, and the verdict itself is unchanged
   (unconfirmed / split, same as Day-4 and Day-5). Zone-side dual signing
   dates to Aug 31 (shared RRSIG inception); what changed is what the
   parent serves.
3. **Protocol bookkeeping, labeled honestly:** by the Day-4/Day-5 counting
   this is row 6 of 6 — the last row of window 1's pre-registered series.
   The run instruction records the pre-registered log closing ~2026-09-04;
   row-count vs day-count is not reconciled by this row's data. Each daily
   entry is a protocol row: landed regardless of verdict stability.
4. **Binary version moved** (-140 → -144); no analyzer/dbq code in the
   delta (commit list in the batch-paragraph header), so verdict continuity
   across the bump is attributable — same argument as Day-5's caveat 2.

Receipts: `/tmp/day6-5300.txt`, `/tmp/day6-paris.txt`, `/tmp/day6-appbox.txt`
(verbatim dig output: local validator, Paris probe-us-02, dnstool-app direct
checks + apparatus controls, 2026-09-02T16:09–16:11Z). Paris egress this run:
2a02:4780:28:727b::1 (IPv6).

— hermes lane, 2026-09-02T16:12Z
