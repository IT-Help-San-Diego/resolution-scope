# Day-16 Decay Entry - 2026-09-12T16:01-16:04Z (t + ~300h)
## Post-series row: Paris + app box + home + batch verdicts, per the Day-0 protocol

**Post-series row.** Ninth post-close confirmation row. Batch `b_1789228879_61e6873e82e2`
(HTTP 202, queued 8/8 failed 0, rows 16:01:57-16:03:28Z, ids 18781-18788,
app_version 26.51.0-144-g385ba8783 — same binary as Days 6-15). **Every batch verdict
byte-identical to Days 6-15.** The Quad9 flaky slot moved back to pq-dualds today
(`secure` -> `unmeasured`), the consensus holding at canonical 3 secure / 2 ad_absent =
`split` — third independent recurrence (Day-12 pq2, Day-14 pq-dualds, today
pq-dualds). Cloudflare's alg-18 validation held day 12: first AD 2026-09-01T16:03:38Z →
still AD=True 2026-09-12 16:02:07Z — **~264h, day 12**. Google, Quad9, OpenDNS, DNS4EU
silent at the wire. Authoritative serials unchanged since Day-0/Day-2. The `:5300`
validator remains DOWN (lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 times
out — Day-12 caveat 1 unchanged, day 6); the authoritative-side serial check continues
as the substitute — receipt in hand, not a claim.

---

### Validator + public vantages

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Authoritative-side check (SUBSTITUTE for dead :5300) | direct serial read via fixture box 44.232.227.144 :53 | serial 2026083011 (16:02Z; unchanged since Day-0) | serial 2026083103 (16:02Z; unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR, no EDE** (`flags: qr rd ra ad`, 16:03:50Z) — **flip persisted >=264h (day 12)** | **AD=True** (`flags: qr rd ra ad`, 16:03:50Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent | AD=False, NOERROR, silent (same) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent | AD=False, NOERROR, silent (same) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (egress 44.226.60.249, 16:02:07Z) | **AD=True** (16:02:07Z) |
| Home line | `1.1.1.1` | **AD=True** (egress 97.170.246.157, ~16:01-16:02Z) | **AD=True** (same) |

Controls: nlnetlabs.nl @1.1.1.1 AD=True from home (~16:01Z) and Paris (16:03:50Z).
Serials 2026083011 / 2026083103 byte-verified unchanged since Day-0 / Day-2 off the
fixture box via authoritative :53 (`flags: qr aa` with +norecurse — authoritative,
non-validating, as expected). RRSIG alg **18** (ML-DSA-44) present on both windows in
the Cloudflare @1.1.1.1 +dnssec receipt (SOA 18 3 3600, expiry 20260929091123 /
20260930002650 — within validity). First Cloudflare AD 2026-09-01T16:03:38Z -> still
AD=True 2026-09-12 16:02:07Z.

### Batch verdicts — specimen battery, 8/8 rows landed (batch
`b_1789228879_61e6873e82e2`, HTTP 202, queued 8/8 failed 0, rows
16:01:57-16:03:28Z, ids 18781-18788; ad_consensus per domain from
`full_results->'dnssec_analysis'`; app_version 26.51.0-144-g385ba8783 —
same binary as Days 6-15)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (5/5) | Unconfirmed |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |
| pqns.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-15 -> Day-16): pq = all five `ad_absent` (identical).
pq2 = all five `ad_absent` (identical; Day-15 also 5/5). pq-dualds =
**Quad9 `secure` -> `unmeasured`** (consensus `split` unchanged — 3 secure /
2 ad_absent by canonical counting of the non-`unmeasured` votes; DNS4EU/OpenDNS
`secure` + Google/Cloudflare `ad_absent`). nlnetlabs.nl = all five `secure`.
The series' single VERDICT-level resolver transition remains the Day-5
Cloudflare event.

### Day-16 verdict

**The Day-5 event held a twelfth full day (~264h, day 12).** Cloudflare's alg-18
validation persisted day 12 (first AD 2026-09-01T16:03:38Z -> still AD=True
2026-09-12 16:02:07Z, ~264h), confirmed from Paris, the instrument box, and
the home line. Google, Quad9, OpenDNS, DNS4EU did not move at the verdict
level. Batch verdicts byte-identical to Days 6-15 — stability IS the finding.
Authoritative serials unchanged.

### Labeled caveats (verification discipline)

1. **`:5300` local validator still DOWN (Day-12 caveat 1, unchanged, day 6).**
lsof empty on TCP and UDP 5300; dig @127.0.0.1 -p 5300 times out. Validator
repair remains the follow-up task — it is the series' only remaining LOCAL
validating vantage. Substituted with the authoritative-side serial check as in
Days 12-15.
2. **Instrument-vs-wire Cloudflare discrepancy replicates, day 11.** Batch
rows (16:01:57-16:03:28Z) again voted Cloudflare `ad_absent` on both alg-18
windows while direct dig @1.1.1.1 from the same box (16:02:07Z), Paris
(16:03:50Z), and home (~16:01Z) all read AD=True. Analyzer-side, not the wire —
now an 11-day-replicated instrument defect, stable across the same binary
(-144) on Days 6-16.
3. **Quad9 flaky-slot watch: THIRD recurrence, back on pq-dualds.** Day-12
re-armed the watch (pq2 `unmeasured`); Day-13 clean; Day-14 pq-dualds
`unmeasured`; Day-15 clean; today pq-dualds `unmeasured` again
(`secure` -> `unmeasured`). Three independent recurrences on record (two
domains, alternating clean/recurred days) = real flakiness, not a
domain-specific artifact. Consensus `split` held through every occurrence.
Low priority.
4. **Row-verification keyed on `scan_source`** (value
`batch:<batch_id>`), not `_request_source` (carried from Day-11;
`_request_source` -> 42703); `ad_consensus`/`dnssec_state`/
`chain_of_trust` live inside `full_results->'dnssec_analysis'` (json — needs
`::jsonb` cast for jsonb functions). 8/8 rows found for this batch_id
(ids 18781-18788).
5. **Egress-labeled receipts (standing requirement).** Paris 76.13.61.227
(IPv4; same path as Days 7-15), home 97.170.246.157, instrument box
44.226.60.249 — every vantage receipt read its egress line before labeling.
6. **Fixture-box SSH still denied (Day-14 caveat 6, unchanged).** Not retried
today; the serial reads again went over DNS to the authoritative :53
(44.232.227.144, `flags: qr aa`). Measurement unaffected. SSH access to
the fixture box remains a follow-up item, not a data defect.
7. **DB access via sudo on the box (ops note, not a defect).** /etc/dnstool/env
is root-only (600); row verification ran through `sudo bash -s` with the
DATABASE_URL parsed by grep+cut (direct `source` fails: env line 8 contains
parentheses — bash syntax error). Documented so future rows don't re-derive
the access path.

Receipts: Paris dig transcripts (16:03:50Z, AD flags + control + Google/
Quad9 silent rows), fixture-box serial reads via authoritative :53
(`flags: qr aa`, 16:02Z), instrument-box and home dig transcripts
(egress-stamped, 16:02:07Z / ~16:01Z), Cloudflare +dnssec RRSIG receipt
(alg 18, expiry 20260929/20260930). DB row dump + per-resolver votes in the
session record, batch `b_1789228879_61e6873e82e2`.
2026-09-12T16:01-16:04Z.

-- hermes lane, 2026-09-12T16:10Z
