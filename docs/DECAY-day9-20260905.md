# Day-9 Decay Entry - 2026-09-05T16:01-16:12Z (t + ~144h)
## Both windows: :5300 + Paris + batch verdicts, per the Day-0 protocol

**Post-series row.** The pre-registered 6-day window-1 log closed at Day-8
(~2026-09-04) per the close instruction; per the standing rule each daily
entry is a protocol row landed regardless of verdict stability -- stability
IS the finding -- so Day-9 is the first post-close confirmation row. Every
verdict is byte-identical to Day-8: no resolver moved since the Day-5
Cloudflare event.

---

### Validator + public vantages (both windows)

| Vantage | Resolver | pq (serial 2026083011) | pq2 (serial 2026083103) |
|---|---|---|---|
| Local validator | `:5300` PowerDNS | **AD=True** (`flags: qr rd ra ad`, DNSKEY ANSWER:2 w/ +dnssec, 16:01-16:03Z; serial unchanged since Day-0) | **AD=True** (SOA+RRSIG ANSWER:2, same receipt set, 16:03Z; serial unchanged since Day-2) |
| Paris probe (probe-us-02) | `1.1.1.1` Cloudflare | **AD=True, NOERROR, no EDE** (16:06:09Z) -- **flip persisted >=96h** | **AD=True, no EDE** (16:06:09Z) |
| Paris probe | `8.8.8.8` Google | AD=False, NOERROR, silent (unchanged) | AD=False, silent (unchanged) |
| Paris probe | `9.9.9.9` Quad9 | AD=False, NOERROR, silent (unchanged) | AD=False, silent (unchanged) |
| Instrument box | `1.1.1.1` from dnstool-app | **AD=True** (direct dig 16:12:33Z -- see caveat 1) | (same, not re-shot today; Day-6/7/8 established) |

Controls sane: nlnetlabs.nl @1.1.1.1 AD=True (Paris ANSWER:3, Mac ANSWER:2
-- RRSIG-presence differences between resolvers are normal; AD agrees);
+cdflag control on :5300 returned `ad cd` today vs Day-8's `ra cd` -- the
+dnssec re-shot at 16:04:24Z shows `ad cd` WITH RRSIG present and AD=True
retained with CD set (non-validating path), so the resolver-side AD bit is
sticky on a cached valid answer regardless of CD; pqns A carries the parent
alg-13 RRSIG with AD=True -- inherited chain intact. pq-dualds at :5300:
dual DNSKEY (`257 3 18` + `257 3 8`), dual RRSIG DNSKEY (keytags 46383-class
+ alg-8 keytag), wire state identical to Day-6/7/8.

### Batch verdicts -- specimen battery, 8/8 rows landed (batch
`b_1788624088_2e7a28c7321f`, HTTP 202, queued 8/8 failed 0, rows
16:02:28-16:03:40Z; ad_consensus per domain from `full_results.dnssec_analysis`;
app_version 26.51.0-144-g385ba8783 -- **same binary as Days 6-8**)

| Domain | dnssec_state | chain_of_trust | ad_consensus | label |
|---|---|---|---|---|
| nlnetlabs.nl | present | complete | secure (5/5 resolvers) | Signed |
| pq2.resolutionscope.com | present | unconfirmed | ad_absent (4/5) | Unconfirmed |
| pq.resolutionscope.com | present | unconfirmed | ad_absent (5/5 resolvers) | Unconfirmed |
| pq-dualds.resolutionscope.com | present | unconfirmed | split (3 secure / 2 ad_absent) | Unconfirmed |
| dane.resolutionscope.com | present | inherited | - | Inherited |
| mx.dane.resolutionscope.com | present | inherited | - | Inherited |
| dane-rx.resolutionscope.com | present | inherited | - | Inherited |
| pqns.resolutionscope.com | present | inherited | - | Inherited |

Per-resolver votes (Day-8 -> Day-9): pq = all five `ad_absent` (identical
to Day-8). pq2 = Cloudflare/Google/OpenDNS/DNS4EU `ad_absent` identical;
**Quad9 moved `ad_absent` -> `unmeasured`** -- a vote-completion delta
(the inverse of Day-8's pq/Quad9 move), not a resolver transition; the
series' single transition remains the Day-5 Cloudflare event. pq-dualds =
Quad9/DNS4EU/OpenDNS `secure` + Google/Cloudflare `ad_absent` (same 3/2
split as Days 6-8). nlnetlabs.nl = all five `secure`.

### Day-9 verdict

**The Day-5 event held a fourth consecutive day.** Cloudflare's alg-18
validation persisted a fourth full day (first AD 2026-09-01T16:03:38Z ->
still AD=True at 16:06:09Z on 2026-09-05, Paris and app-box alike; no EDE-1
anywhere). Google, Quad9, OpenDNS, DNS4EU did not move -- the series'
single transition remains bounded to (2026-08-31T16:03Z,
2026-09-01T16:03:38Z]. For the four unchanged behaviors, Day-9 is a
stability row. Both windows byte-stable at :5300 (serials 2026083011 /
2026083103 unchanged since Day-0 / Day-2).

### Labeled caveats (verification discipline)

1. **Instrument-vs-wire Cloudflare discrepancy replicates, day 4.** Batch
rows (16:02:28-16:03:40Z) again voted Cloudflare `ad_absent` on both
alg-18 windows while direct dig @1.1.1.1 from the same box (16:12:33Z)
and Paris (16:06:09Z) both read AD=True. Per the Day-6/7/8 finding this is
analyzer-side, not the wire -- now a 4-day-replicated instrument defect,
stable across the same binary (-144) on Days 6, 7, 8, and 9. Logged
because replication depth is part of the measurement record.
2. **pq2/Quad9 vote went ad_absent -> unmeasured** -- a vote-incompletion
delta, not a resolver transition; the series' single transition remains
the Day-5 Cloudflare event. All other votes identical to Day-8. The two
independent vote slots that flickered across Days 8/9 (pq/Quad9, pq2/
Quad9) are both Quad9 -- if the per-resolver probe keeps flaking there,
flag it for the batch runner as a possible Quad9-path timeout.
3. **Row-verification keyed on `scan_source`** (values
`batch:<batch_id>`), not `_request_source` -- the column `_request_source`
does not exist on domain_analyses (verified live: bare-string predicate ->
ERROR 42703). 8/8 rows found for this batch_id.
4. **Post-series status.** Day-9 is the first entry past the pre-registered
close. Any Day-10+ entry continues the post-series confirmation series;
flag if the instrument changes.

Receipts: `/tmp/day9-5300.txt`, `/tmp/day9-5300b.txt`, `/tmp/day9-paris.txt`,
`/tmp/day9-appbox.txt` (verbatim dig output: :5300 validator, Paris
probe-us-02, dnstool-app direct checks + apparatus controls,
2026-09-05T16:01-16:12Z). Paris egress this run: 76.13.61.227 (IPv4; same
path as Day-7/8).

-- hermes lane, 2026-09-05T16:15Z
